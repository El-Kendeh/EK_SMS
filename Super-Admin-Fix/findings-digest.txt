### [1] CRITICAL · confirmed · area=grade-integrity · cat=backend-bug
TITLE: Entire grade-integrity chain reads a table no code ever writes - permanently empty USP
PAGE: grade-integrity  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeIntegrity.js, SAGradeReport.js  | BACKEND: GET /api/grade-alerts/ -> getGradeAlerts (superadminController.js:343)
EVIDENCE: getGradeAlerts queries SystemOpsAlert where `trigger_type: { [Op.like]: 'grade%' }` (superadminController.js:345-349). A full grep of backend/src shows SystemOpsAlert is only ever READ - there is NO `SystemOpsAlert.create` and no seeder anywhere (only refs: superadminController.js:345 findAll, superadminDataController.js:570 findAll, :593 findByPk). Nothing in the app emits a grade-type alert (teacher grade-mod requests do not write here). So the `sa_system_ops_alerts` table has zero `grade%` rows in any real deploy, and /api/grade-alerts/ always returns `{alerts: []}`. SAGradeIntegrity (the Integrity Dashboard, the platform USP), SAGradeReport's stats/chart/top-schools/reasons/recent-logs, and the audit drill-down all consume this empty array, so every one of these pages renders its empty state forever. This is mock-data infrastructure presented as a live integrity console.
IMPACT: The flagship 'Grade Integrity' surface - the stated platform USP - is non-functional: no grade-modification request will ever appear, no anomaly is ever surfaced, the SA can never review/validate/flag a grade change. The feature is dead end-to-end despite looking complete.
FIX: Wire a real source: when a teacher/school-admin submits a grade-modification request (or when the event-sourced grade log records an out-of-band change), write a SystemOpsAlert with trigger_type='grade_modification' (or query the actual grade-change/audit-log tables directly in getGradeAlerts instead of the ops-alert table). Until a real source exists, the nav rows should show an explicit 'not yet collecting data' state rather than an empty list that looks broken.
VERIFY-NOTE: Accurate but understated. The defect is not merely 'a table no code writes' — the real grade-modification data DOES exist and is actively written, just to a different table the SA console never reads. Teacher/parent/student grade-change requests are persisted to pruh_core_modification_request (ModificationRequest model) via ModificationRequest.create (teacherController.js:1982, parentController.js:777, studentController.js:1294) and reviewed by school admins (schoolController.js:2467). Meanwhile /api/grade-alerts/ reads the unrelated, never-populated sa_system_ops_alerts (SystemOpsAlert) table. Correct fix is to point getGradeAlerts at pruh_core_modification_request (filtered/joined for grade-type requests) instead of SystemOpsAlert, so SAGradeIntegrity, SAGradeReport and SANotifications surface the real, already-captured grade-change activity. Confirmed: no seeders directory and no migration seeds the alerts table either.

### [2] CRITICAL · confirmed · area=reports-analytics · cat=backend-bug
TITLE: getSchoolStats returns hardcoded zero counts for every school — all headcount tiles/cards are fake
PAGE: analytics / benchmarks  | COMPONENT: EK_SMS/src/components/superadmin/SAAnalytics.js; EK_SMS/src/components/superadmin/SABenchmarks.js  | BACKEND: GET /api/school-stats/ -> superadminDataController.getSchoolStats
EVIDENCE: superadminDataController.js:412-420 builds the response by mapping School rows to a literal object `{ school_id, student_count: 0, teacher_count: 0, active_classes: 0, attendance_rate: 0, avg_performance: 0 }` — no query against Student/Teacher tables at all. There is no SQL that counts anything. Frontend consumes these as real: SAAnalytics.js:623-636 sums `s.student_count` into the 'Total Students' KPI (always 0), and SchoolCard (290-310) renders `students`/`teachers` from `statsData` (always 0). SABenchmarks.js:228-247 ranks schools by `s.student_count` and sums `student_count`/`teacher_count` into Platform Summary.
IMPACT: Every student/teacher number the Super Admin sees on Analytics and Benchmarks is a fabricated 0. 'Total Students' KPI, school-card headcounts, oversight Overview, Top-Schools perf index, and Platform Summary (Active Teachers / Students Enrolled) are all meaningless. This is the platform's monitoring USP showing zeros while real schools have enrolled students.
FIX: Implement getSchoolStats for real: COUNT students and teachers grouped by school_id (Student.count / Teacher.count, or a GROUP BY query joined to School), and include is_approved + school_name (see next finding). Return real active_classes/attendance/avg_performance or omit them so the UI shows '—' instead of fake 0.
VERIFY-NOTE: Finding is accurate. Minor amplification (makes it worse, not better): the stats objects returned by getSchoolStats contain neither `is_approved` nor `school_name`. So in SABenchmarks.js line 198 `data.stats.filter(s => s.is_approved)` drops ALL schools (is_approved is undefined on every object), leaving schoolStats empty — so its Platform Summary totals and Top-Schools list render as empty/blank rather than literal 0, and realTopSchools' s.school_name (line 230) would be undefined even if rows survived. SAAnalytics correctly shows literal 0s as the finding states. Either way all headcounts are wrong.

### [3] CRITICAL · confirmed · area=reports-analytics · cat=data-mismatch
TITLE: Benchmarks reads is_approved + school_name from /api/school-stats/, but getSchoolStats returns neither — whole Benchmarks data layer is permanently empty
PAGE: benchmarks  | COMPONENT: EK_SMS/src/components/superadmin/SABenchmarks.js  | BACKEND: GET /api/school-stats/ -> superadminDataController.getSchoolStats
EVIDENCE: SABenchmarks.js:196-202 does `data.stats.filter(s => s.is_approved).sort((a,b)=> b.student_count - a.student_count)`. getSchoolStats (superadminDataController.js:413-420) returns objects with ONLY {school_id, student_count, teacher_count, active_classes, attendance_rate, avg_performance} — `is_approved` is undefined on every row, so the filter removes ALL rows and `schoolStats` is always `[]`. Line 230 also reads `s.school_name`, which getSchoolStats never returns (it only selects ['id','name'] at 412 and maps to `school_id`).
IMPACT: Top Performing Schools always renders 'No approved schools on the platform yet.' (320), Schools Live KPI and Platform Summary all show '—', and Compare needs >=2 schools so it is also permanently unreachable — regardless of how many approved schools exist. The entire Benchmarks page is dead data even on a populated platform.
FIX: Add is_approved and school_name to each getSchoolStats row (the School query at 412 should select is_approved and name). Then the existing Benchmarks filter/sort/render works.
VERIFY-NOTE: The finding is accurate. One small overstatement: the title says the 'whole Benchmarks data layer is permanently empty,' but the page also calls /api/grade-stats/ independently, which populates Pass Rate, Avg Score, Total Grades KPIs and the Grade Distribution chart. Only the school-stats-derived sections are permanently dead: Top Performing Schools, Schools Live KPI, Compare view, and the Schools/Teachers/Students tiles of Platform Summary. Additionally, even the is_approved filter aside, getSchoolStats returns hardcoded 0 for student_count/teacher_count and never returns school_name, so the school table would be empty/unnamed regardless — the filter is just the first of two failures. Net effect is unchanged: the entire school-benchmarking half of the page is non-functional.

### [4] CRITICAL · confirmed · area=shell-nav · cat=ui-not-functional
TITLE: Entering a school does not switch the console — SA shell keeps rendering after enterSchool() swaps the token
PAGE: __enter_school / schools  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js + EK_SMS/src/App.js  | BACKEND: POST /api/impersonate/ -> superadminController.impersonate (works, returns {success,token,user})
EVIDENCE: enterSchool() (SuperadminDashboard.js:879-894) does localStorage.setItem('token', data.token); localStorage.setItem('user', JSON.stringify(data.user)); setEnterSchoolOpen(false); window.dispatchEvent(new Event('storage')). But App.js (App.js:178-194) renders <SuperadminDashboard> with NO key tied to token/user and App registers NO 'storage' listener (only popstate at App.js:97). The Dashboard's auth effect that reads localStorage user runs once on mount keyed on stable callbacks (SuperadminDashboard.js:492-509) and never re-runs. So after the token swap the same Dashboard instance keeps its old superadmin `user` state and keeps rendering the SA shell/nav instead of the impersonated school_admin console. Contrast: UserContext (UserContext.js:22) and SchoolBrandingContext (SchoolBrandingContext.js:124) DO listen to 'storage', so branding/user-context update while the actual dashboard surface does not — an inconsistent half-switched state.
IMPACT: The core SA workflow step 'Enter a school' (impersonation launcher) appears to do nothing: operator picks a school, modal closes, but the page still shows the superadmin menu and superadmin data. The school console only appears after a manual full page reload. Primary impersonation entry point is effectively broken from the shell.
FIX: Have App.js listen for the 'storage' event and re-read localStorage user/token to recompute currentPage, and/or pass a key={token} (or a userVersion counter) to <SuperadminDashboard> so it remounts and its auth effect re-reads the new impersonated user. The exit path (ImpersonationBanner.handleReturn) has the same reliance on the dispatched 'storage' event and needs the same App-level listener to take effect without a reload.

### [5] CRITICAL · confirmed · area=years-terms · cat=backend-bug
TITLE: SystemAcademicYear model uses status + deleted_at columns that no migration creates — every academic-year query 500s in prod
PAGE: academic-year  | COMPONENT: EK_SMS/src/components/superadmin/SARefDataManager.js (Academic Years mode), EK_SMS/src/components/superadmin/SACreateTerm.js  | BACKEND: GET/POST/PUT/DELETE /api/academic-years/* -> getAcademicYears/createAcademicYear/updateAcademicYear/deleteAcademicYear/toggleAcademicYearStatus/rolloutAcademicYear/restoreAcademicYear/cloneAcademicYear/closeAcademicYear in superadminDataController.js
EVIDENCE: models/SystemAcademicYear.js:10-15 declares `status: ENUM('draft','active','closed','archived')` and `deleted_at: DATE`. Every controller fn reads/writes these: getAcademicYears (superadminDataController.js:840 `where = {deleted_at:null}`, :875-876 maps `status`/`deleted_at`), createAcademicYear (:904 `status:'draft'`), deleteAcademicYear (:995-996 sets `deleted_at`+`status='archived'`), toggleAcademicYearStatus (:1027/:1031/:1038 `status`), rolloutAcademicYear (:1071/:1075), restoreAcademicYear (:1205-1206), cloneAcademicYear (:1257), closeAcademicYear (:1295). BUT both schema sources omit these columns: migrations/2026-05-18-all-new-tables.sql:479-487 (`pruh_system_academicyear` has only id,name,start_date,end_date,is_active,created_at,updated_at) and migrations/run-all-tables.js:430-438 (identical). No ALTER TABLE adding `status`/`deleted_at` exists anywhere (grep of *.sql). index.js:154 runs `db.sync({ alter: process.env.NODE_ENV !== 'production' })` — so in production the schema is NOT auto-altered. Because Sequelize findAll selects all model attributes, even the page-load `getAcademicYears` issues `SELECT ... status, deleted_at ...` -> MySQL ER_BAD_FIELD_ERROR -> caught at :884-886 -> 500 `Internal server error`.
IMPACT: In production the entire Academic Years page fails to load (list GET 500s), the SACreateTerm year dropdown shows 'Could not load academic years', and every lifecycle action (create/archive/toggle/rollout/restore/clone/close) 500s. The year+term subsystem is completely non-functional in prod until a manual ALTER runs.
FIX: Ship a migration: `ALTER TABLE pruh_system_academicyear ADD COLUMN status ENUM('draft','active','closed','archived') NOT NULL DEFAULT 'draft', ADD COLUMN deleted_at DATETIME NULL;` Add it to migrations/run-all-tables.js (and a dated .sql) so prod deploy applies it, and backfill existing rows (status='active' where is_active=1 else 'draft').

### [6] CRITICAL · unverified · area=settings-profile · cat=security
TITLE: Global platform settings (/admin-settings/) are writable by ANY authenticated user — not super-admin gated
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: GET/PATCH /api/admin-settings/ -> getAdminSettings/patchAdminSettings
EVIDENCE: routes/superadmin.js:107-108 mounts `router.get('/admin-settings/', data.getAdminSettings)` and `router.patch('/admin-settings/', data.patchAdminSettings)` on the BASE router, BEFORE the superadmin sub-router `const sa = express.Router(); sa.use(requireRole(['superadmin']))` at line 111-112. The base router only has `router.use(authenticateToken); router.use(schoolScope)` (lines 95-96), and schoolScope.js never blocks any role (it only sets req.schoolId). patchAdminSettings (superadminDataController.js:270-285) has no internal role check — it blindly does `merged = { ...parsed, ...incoming }; row.settings_json = JSON.stringify(merged)` on the single global row (id:1, loadSettings:111-123). So a teacher/parent/student/school-admin token can read AND overwrite platform_name, maintenance_mode, allow_registrations, branding_logo/favicon URLs, security flags, audit retention, etc.
IMPACT: Any logged-in user of any tenant can flip the whole platform into maintenance mode, close registrations, change platform branding URLs, or read the super admin's stored phone/bio — a cross-tenant privilege escalation and integrity hole.
FIX: Move the /admin-settings/ GET+PATCH routes onto the `sa` router (after requireRole(['superadmin'])), or add an explicit `if (req.user.role !== 'superadmin') return res.status(403)` guard inside getAdminSettings/patchAdminSettings. Keep /profile/ and /change-password/ shared (they operate on req.user.id) but gate the global settings row.

### [7] HIGH · confirmed · area=grade-integrity · cat=data-mismatch
TITLE: Controller returns 6 flat fields; cards and audit detail read ~15 nested fields it never sends
PAGE: grade-integrity  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeIntegrity.js:52-130, SAGradeReport.js:278-292  | BACKEND: getGradeAlerts (superadminController.js:362-369)
EVIDENCE: getGradeAlerts maps each row to ONLY `{ id, status, school, student, subject, requester:{name} }` (superadminController.js:362-369). But RequestCard reads req.oldGrade, req.oldScore, req.newGrade, req.newScore (lines 89,94,96), req.urgency (line 53), req.term (line 82), req.reason (line 112), req.requester.initials (line 118) and req.ts (line 123) - none of which the controller emits. SAGradeReport's recentLogs likewise reads a.oldGrade/a.newGrade/a.urgency/a.reason/a.ts (lines 268,283-289). Result: even if a grade row existed, every card would show `—` scores, blank reason, blank requester initials, blank timestamp, and the +pts delta, urgency accent, term, and reasons-donut would all be empty/wrong. The CSV export (SAGradeIntegrity.js:244-261) would emit blank Old/New Grade/Score and Hash Match columns. `subject` is just `bodyText.slice(0,120)` (superadminController.js:367) - the first 120 chars of the alert body rendered under a 'Subject' label.
IMPACT: The request cards, the audit detail comparison, the reasons donut, the CSV/PDF/Excel exports, and the recent-logs table are structurally incapable of showing real grade data even after the source is fixed - the API contract and the UI contract do not agree.
FIX: Define one canonical alert DTO (id, status, urgency, school, student, subject, term, oldGrade, oldScore, newGrade, newScore, reason, requester:{name,initials,role}, ts) and have getGradeAlerts populate every field the cards/detail/exports read; or trim the UI to only the fields the backend can supply. Do not ship a card that reads 9 fields the endpoint never returns.
VERIFY-NOTE: Mostly accurate. Two refinements: (1) The hash/blockchain badge the auditor cites as read on the card was already removed (SAGradeIntegrity.js:66-68 comment), and the Hash-Verified stat was removed from the report (248-249) — so those specific hashMatch reads are partially mitigated, though the CSV 'Hash Match' column (257) and SAGradeReport isFlag test (289) still reference the never-emitted field. (2) The finding cites the audit-detail reads in SAGradeReport, but the richest broken consumer is actually SAGradeAuditDetail.js, which reads request.oldGrade/newGrade/oldScore/newScore/ts/reason/requester.{initials,ip,device,location}/hashMatch/blockNum from the same raw alert object — all fall back to em-dashes/defaults because the controller emits none of them. Core defect (6 flat emitted vs ~15 consumed; blank scores/reason/urgency/term/ts/initials and broken exports) is fully real and unmitigated.

### [8] HIGH · confirmed · area=people-access · cat=data-mismatch
TITLE: Create-User modal offers Exam Officer / Finance Officer roles that the backend silently downgrades to School Admin
PAGE: users  | COMPONENT: EK_SMS/src/components/superadmin/SAUsers.js  | BACKEND: POST /api/users/ -> superadminDataController.postUsers (uses utils/roleIds.mapInviteLabelToCode)
EVIDENCE: SAUsers.js:53 `const ROLES_LIST = ['Super Admin', 'School Admin', 'Teacher', 'Exam Officer', 'Finance Officer'];` is fed to the role <select> (SAUsers.js:168). On submit, postUsers calls mapInviteLabelToCode(role) (superadminDataController.js:371). roleIds.js:15-25 only maps 'Super Admin'/'School Admin'/'Teacher'/'Parent'/'Principal'/'Bursar'/'Student' and `return 'schooladmin'` as the default. There is NO mapping for 'Exam Officer' or 'Finance Officer', and seedRoles.js:13-21 seeds only superadmin/teacher/student/parent/bursar/schooladmin/principal — no exam/finance role exists. So inviting an 'Exam Officer' or 'Finance Officer' creates a user with role_id of schooladmin.
IMPACT: An operator who invites an Exam Officer or Finance Officer unknowingly grants that account School Admin role_id (full tenant-admin authority), a privilege escalation versus the label shown. The two roles are also not real platform roles at all.
FIX: Either remove 'Exam Officer'/'Finance Officer' from ROLES_LIST until real roles exist, or add explicit mappings in mapInviteLabelToCode plus seed the corresponding roles, and make the default branch reject unknown labels (400) instead of silently falling back to schooladmin.

### [9] HIGH · confirmed · area=people-access · cat=ui-not-functional
TITLE: Invite User 'School' is required free-text but is silently ignored unless it exactly matches an existing school name
PAGE: users  | COMPONENT: EK_SMS/src/components/superadmin/SAUsers.js  | BACKEND: POST /api/users/ -> superadminDataController.postUsers
EVIDENCE: Modal validates `if (needsSchool && !form.school.trim()) e.school = 'School is required for this role'` (SAUsers.js:68) and sends `school: form.school` as a typed string (SAUsers.js:82). Backend only links a school when `role === 'School Admin'` AND an exact name match is found: `const sch = await School.findOne({ where: { name: school } }); if (sch) await SchoolAdmin.create(...)` (superadminDataController.js:383-385). For Teacher/Exam/Finance roles the `school` value is accepted by the form but never used at all. For School Admin, a typo or near-match name finds no school and the SchoolAdmin link is silently skipped with no error returned.
IMPACT: Operator types a school, the call returns success, but the user has no school association (and for non-SA roles the field is pure theatre). The created account is orphaned and the 'School' the operator chose is lost with zero feedback.
FIX: Replace the free-text school field with a dropdown populated from GET /api/superadmin/schools/ (send school_id, not name). In postUsers, if a school is required for the role and the id resolves to nothing, return a 400 instead of creating an unlinked user.
VERIFY-NOTE: Accurate core claim, but two facts strengthen it: (a) The UI's ROLES_LIST offers "Exam Officer" and "Finance Officer", yet mapInviteLabelToCode has no mapping for those labels, so they default to roleCode 'schooladmin' — these invites create a School Admin account, not the chosen role, regardless of the school issue. (b) For ALL non-School-Admin roles the typed school is dropped with zero feedback; for School Admin a typo/near-match school name skips the SchoolAdmin link silently. In every case the API responds success:true and the user is created is_active:false (cannot log in until activated). So the impact is silent creation of orphaned or wrong-role privileged accounts, not just a lost UI field.

### [10] HIGH · confirmed · area=people-access · cat=workflow-gap
TITLE: Invited users are created is_active:false with no activation/credential-delivery path, yet UI claims credentials were emailed
PAGE: users  | COMPONENT: EK_SMS/src/components/superadmin/SAUsers.js  | BACKEND: POST /api/users/ -> superadminDataController.postUsers
EVIDENCE: postUsers sets `is_active: false` and stores a random tempPass (superadminDataController.js:367,379) but never emails anything — there is no sendInvite/sendPasswordResetEmail call in the function (compare resetUserPassword in superadminController.js:476-479 which DOES email). The modal's success state asserts 'Credentials have been dispatched to <email>' (SAUsers.js:150) and the list toast says 'Invitation sent — user will receive credentials by email.' (SAUsers.js:553).
IMPACT: Every invited user lands inactive and never receives credentials, so they can never log in, while the SA is told the invite was emailed. The whole 'Invite User' flow produces dead accounts. getUsers also returns these as status:'inactive' with no way to activate them from this page.
FIX: Send an invite/credential email (or set up a first-login activation token) in postUsers, and either create the user active or add an Activate action. Do not claim 'credentials dispatched' in the UI unless an email actually went out.

### [11] HIGH · confirmed · area=reports-analytics · cat=ui-not-functional
TITLE: Reports Hub Schools export hits /api/superadmin/sa/export/ which does not exist — primary CSV/JSON download 404s
PAGE: reports  | COMPONENT: EK_SMS/src/components/superadmin/SAReportsHub.js  | BACKEND: real route is GET /api/sa/export/ (routes/superadmin.js:132, router mounted at '/api' in index.js:148)
EVIDENCE: SAReportsHub.js:95-96 calls `downloadAuth('/api/superadmin/sa/export/?format=csv...')` and `...?format=json...`. The superadmin router is mounted at app.use('/api', superadminRouter) (index.js:148), and the export route is `sa.get('/sa/export/', ...)` (superadmin.js:132), so the real URL is `/api/sa/export/`. The component prefixes `/api/superadmin/` which resolves to nothing — there is no `/api/superadmin` mount anywhere in index.js. downloadAuth (20-30) throws on !r.ok, surfacing 'HTTP 404' in the error banner.
IMPACT: The headline Reports Hub feature — exporting the Schools Register as CSV/JSON — is broken. Clicking Download CSV/JSON on the Schools card always fails with an HTTP 404 banner. (The Users CSV and Grade-stats JSON exports use correct paths and work; only the Schools export, which is the one backed by the real /sa/export/ endpoint, is mis-pathed.)
FIX: Change both paths in SAReportsHub.js:95-96 from '/api/superadmin/sa/export/' to '/api/sa/export/'.

### [12] HIGH · unverified · area=grade-integrity · cat=backend-missing
TITLE: Audit detail renders hardcoded evidence files, fake blockchain ledger, and fabricated actor IP/device/location
PAGE: grade-audit  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeAuditDetail.js  | BACKEND: n/a (no endpoint - all data is props or hardcoded literals)
EVIDENCE: SAGradeAuditDetail takes only the `request` object passed from the list (no fetch). The Digital Evidence section hardcodes two fake files `Exam_Scan.pdf` (2.4 MB) and `Mod_Request.docx` (145 KB) for EVERY record (lines 252-253). Actor IP/device/location come from request.requester which getGradeAlerts never populates, so ActorCard always shows the 'N/A' fallback (lines 89-90) while the 'External' red-flag logic keys off IP prefixes 45./103. (line 100) that can never appear. The Blockchain Ledger block renders request.blockHash/blockNum/prevHash (lines 282-300) which no backend emits, so it always falls to 'Record was not committed to the blockchain' (line 305). Validate (line 333 `onClick={() => setValidated(true)}`) and Flag (line 317 `onClick={() => setFlagged(true)}`) only flip local useState - no API call, nothing persists.
IMPACT: An SA opening any grade-audit record sees fabricated forensic evidence (same two file names every time), a permanently 'not committed to blockchain' warning, and Validate/Flag buttons that appear to act but change nothing on refresh. For the platform's integrity USP this is misleading.
FIX: Either build the backing model (evidence attachments, a real grade-change ledger, persisted validate/flag actions writing to the grade audit log) and fetch the record by id, or gate this whole detail behind the feature being real. The honesty banner (lines 128-134) is good but does not excuse shipping a clickable Validate/Flag that no-ops; disable them or wire them to POST.

### [13] HIGH · unverified · area=notifications · cat=data-mismatch
TITLE: Grade-alert notifications read fields the controller never returns (urgency / ts / oldGrade / newGrade)
PAGE: notifications  | COMPONENT: EK_SMS/src/components/superadmin/SANotifications.js  | BACKEND: GET /api/grade-alerts/ -> getGradeAlerts (EK_SMS/backend_node/src/controllers/superadminController.js:343)
EVIDENCE: buildNotifications reads a.urgency (SANotifications.js:68,69), a.subject/a.oldGrade/a.newGrade (SANotifications.js:72-73), and a.ts (SANotifications.js:80). But getGradeAlerts maps each row to ONLY { id, status, school, student, subject, requester } (superadminController.js:362-369) — there is no urgency, oldGrade, newGrade, or ts field. So every grade notification: (a) title always falls to the generic 'Grade modification alert' (urgency undefined, line 68-70), (b) severity always falls back to 'info' (SEVERITY_MAP[undefined], line 78), (c) body always renders 'unknown subject — ? → ?' because oldGrade/newGrade are undefined (line 73), and (d) timestamp = a.ts = undefined, so `new Date(undefined)` is Invalid Date -> relativeTime() returns 'NaNm ago' (line 186-194/400) and the final sort by timestamp (line 130) is corrupted for every grade row. Note `subject` IS returned but holds the first 120 chars of the alert body (superadminController.js:367), not a school subject, so even the subject text is wrong.
IMPACT: Every grade-alert notification shows a generic 'info' card with a meaningless '? → ?' body and a broken 'NaNm ago' timestamp, and pollutes the sort order of the whole feed. The grade-integrity signal — the platform's stated USP — is effectively unreadable in Notifications.
FIX: Either extend getGradeAlerts to return urgency, old_grade, new_grade and a real timestamp (ts/created_at) on each alert object, or change buildNotifications to consume the fields the controller actually emits (status, student, school, requester.name, and an added timestamp). Add a guard so timestamp is never undefined before relativeTime().

### [14] HIGH · unverified · area=rejected · cat=workflow-gap
TITLE: Reconsider fires an irreversible state change + email on a single click, no confirmation
PAGE: rejected  | COMPONENT: EK_SMS/src/components/superadmin/SARejected.js / EK_SMS/src/components/superadmin/SARejectionAudit.js / EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: POST /api/schools/approve/ -> handleSchoolAction (request_changes branch), superadminController.js:192-234
EVIDENCE: SARejected.js:205-210 the Reconsider button does `onClick={() => onReconsider && onReconsider(school)}`; SARejectionAudit.js:357-363 same. In SuperadminDashboard.js:574-576 `handleReconsider` calls `handleAction(school.id, 'request_changes', 'Reconsidering rejected application')` directly with NO confirm modal. handleAction (SuperadminDashboard.js:530-546) immediately POSTs the action. The backend (superadminController.js:210-224) then sends a `sendSchoolChangeRequestEmail` to every school admin. Compare with reject/approve which go through ConfirmModal/ApproveModal in SAReview.js:197-209. So one stray click on the Rejected card both mutates the school out of the Rejected bucket and emails the school, with no undo.
IMPACT: An accidental click silently un-rejects a school and emails its admins asking them to update their application. There is no confirmation step for a state-changing, email-sending action, unlike every other approval action in the console.
FIX: Route Reconsider through a ConfirmModal (reuse SAReview's pattern) that lets the operator add a note before it POSTs request_changes and sends the email.

### [15] HIGH · unverified · area=settings-profile · cat=ui-not-functional
TITLE: Security tab toggles (Global 2FA Enforcement, Auto Grade Locking, Session Timeout) persist but are never enforced
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: PATCH /api/admin-settings/ -> patchAdminSettings
EVIDENCE: SASettings.js:741-784 renders 'Global 2FA Enforcement' (twoFA), 'Auto Grade Locking' (autoLock) and 'Session Timeout' slider, and saveSecuritySettings (lines 639-654) PATCHes them into admin-settings JSON. But these are only stored as flags — nothing in the backend reads settings.twoFA / settings.autoLock / settings.sessionTimeout to actually enforce 2FA, lock grades after 24h, or expire sessions. The toggles present as live security controls (no honesty banner, unlike the Lockdown/Backup cards which DO disclose 'not enforced').
IMPACT: An operator believes they have enforced global 2FA / auto grade-locking / a session timeout, but logins, grades, and sessions are completely unaffected. False sense of security on the platform's headline (grade-integrity) controls.
FIX: Either wire these flags into the auth/grade/session middleware so they take effect, or add an explicit 'Recorded — enforcement pending' disclosure on the Security Configuration card the same way LockdownActive (line 477-479) and the Backups tab (line 1155-1158) already do.

### [16] HIGH · unverified · area=settings-profile · cat=ui-not-functional
TITLE: Bulk Export offers PDF format + Grades/Audit/Users datasets that the backend silently ignores
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: GET /api/sa/export/ -> getSaExport
EVIDENCE: SASettings.js:962-989 offers four dataset checkboxes (schools/grades/audit/users) and three formats (CSV/JSON/PDF), and the request sends `datasets=...&format=pdf` (lines 1215-1219). But getSaExport (superadminDataController.js:738-776) IGNORES req.query.datasets entirely and ALWAYS queries only School rows; for format it handles only 'json', and anything else (including 'pdf') falls through to the CSV branch (lines 764-771) returning `Content-Type: text/csv` + filename `eksms_export.csv`. So selecting Grades/Audit/Users still exports the schools list, and selecting PDF downloads a CSV.
IMPACT: An operator exporting 'Audit Logs as PDF' for a compliance request actually receives the schools master list as a CSV — wrong data, wrong format, no error. Compliance/audit workflow is broken and misleading despite the partial in-modal callout.
FIX: Implement per-dataset export (grades/audit/users) and a real PDF path in getSaExport, OR disable the unimplemented dataset checkboxes and the PDF format button in the UI until backed. The honesty note at line 1205 should not substitute for not shipping non-functional options.

### [17] HIGH · unverified · area=system-security · cat=workflow-gap
TITLE: Forensics page is permanently empty — no code ever creates a ForensicEvent row
PAGE: forensics  | COMPONENT: EK_SMS/src/components/superadmin/SAForensics.js  | BACKEND: GET /api/forensic-events/ -> superadminDataController.getForensicEvents (line 494)
EVIDENCE: getForensicEvents (superadminDataController.js:494-515) reads `ForensicEvent.findAll(...)`, but a repo-wide search for `ForensicEvent.create` returns ZERO hits — no controller, middleware, or job ever inserts a ForensicEvent. The model exists (models/ForensicEvent.js) and the GET is wired, but the table is never written. SAForensics.js:246-267 maps `data.events`; with an always-empty array the page renders only the empty state (SAForensics.js:335-341 'No forensic incidents recorded') and every stat (Open/High/Resolved/Avg, lines 295-307) is permanently 0/'—'. The security-log drill-through (handleForensic -> initialEvent, SAForensics.js:282-289) can therefore never match a case, so 'View forensic detail' from a security event always lands back on an empty list.
IMPACT: An entire top-level System nav page (Forensics, the forensic drill-down from security events) is dead: it can never show data because nothing in the platform produces forensic events. The forensic drill-through button on every security-log row leads nowhere.
FIX: Either emit ForensicEvent rows from the real signals that warrant them (e.g. grade-lock write attempts, hash-mismatch detections, repeated login failures) at the point they occur, or remove the Forensics nav entry until a producer exists. At minimum, derive forensic cases from existing high/critical SecurityAuditLog rows so the page reflects real data.

### [18] HIGH · unverified · area=system-security · cat=workflow-gap
TITLE: Change Alerts page is permanently empty — no code ever creates a SystemOpsAlert row
PAGE: change-alerts  | COMPONENT: EK_SMS/src/components/superadmin/SAChangeAlerts.js  | BACKEND: GET/POST /api/system-alerts/ -> superadminDataController.getSystemAlerts (line 568) / postSystemAlerts (line 589)
EVIDENCE: getSystemAlerts (superadminDataController.js:568-587) reads `SystemOpsAlert.findAll`, and postSystemAlerts only ever UPDATES an existing row (findByPk + set status, lines 593-599). A repo-wide search for `SystemOpsAlert.create` returns ZERO hits — nothing creates these alerts for the documented triggers (grade_lock_attempt, enrollment_change, fee_payment, attendance_anomaly, permission_change defined in SAChangeAlerts.js:5-67). So `data.alerts` is always [], the page shows only the empty state (SAChangeAlerts.js:613-624 'No system alerts yet... will appear automatically when trigger conditions are met') and the acknowledge/resolve POST handlers (lines 481-489) have nothing to act on.
IMPACT: The 'System-Wide Change Alerts' page advertises real-time cross-platform change monitoring but can never display a single alert. Its acknowledge/resolve workflow, channel badges, and filter tabs are unreachable in practice.
FIX: Insert SystemOpsAlert rows at the real trigger sites (grade lock attempts, enrollment status changes, large fee payments, permission changes) so the documented trigger_type taxonomy is actually produced. Until a producer is wired, the empty-state copy overpromises ('will appear automatically').

### [19] HIGH · unverified · area=system-security · cat=backend-missing
TITLE: 'Send Now' broadcast only writes a DB row labelled 'sent' — no email/SMS/push is ever dispatched
PAGE: alert-broadcast  | COMPONENT: EK_SMS/src/components/superadmin/SAAlertBroadcast.js  | BACKEND: POST /api/broadcast-alerts/ -> superadminDataController.postBroadcastAlerts (line 538)
EVIDENCE: The compose UI presents a primary 'Send Now — {target}' button (SAAlertBroadcast.js:263-268) and a success screen 'Alert Sent!' (lines 167-173). But postBroadcastAlerts (superadminDataController.js:538-565) only does `BroadcastAlert.create({ ... status:'sent', sent_at:new Date() })` plus an audit-log row — it never calls sendEmail/Resend, SMS, or push. No school admin actually receives anything; the row is just persisted with a 'sent' label. The audience/target (All Schools, Region: Western, Admin Staff — SAAlertBroadcast.js:227) is stored as a free-text string and never resolved to recipients.
IMPACT: Super admin believes a platform-wide announcement was delivered to school administrators, but nothing leaves the server. Critical security broadcasts silently reach no one.
FIX: On postBroadcastAlerts, resolve the audience to actual users and dispatch via the existing email path (sendPasswordResetEmail's Resend integration is already imported elsewhere), and/or create per-user in-app notifications. Only mark status:'sent' after dispatch; otherwise label it 'recorded'.

### [20] MEDIUM · confirmed · area=geography · cat=backend-bug
TITLE: Newly added countries/regions/cities are created is_active=false and never appear in registration dropdowns
PAGE: countries / regions / cities  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js (geography configs L1322-1362); EK_SMS/src/components/Register.js  | BACKEND: POST /api/countries|regions|cities -> createCountry/createRegion/createCity
EVIDENCE: All three models default is_active to false: Country.js:7, Region.js:8, City.js:9 (`is_active: { type: DataTypes.BOOLEAN, defaultValue: false }`). The create handlers never set is_active: createCountry (superadminDataController.js:1943 `Country.create({ name })`), createRegion (:2023), createCity (:2115) all omit is_active, so every new row is INACTIVE. The downstream consumer filters on active: Register.js:1391 `(r.countries||[]).filter(c => c.is_active)`, :1415 `(res.regions||[]).filter(r => r.is_active)`, :1440 `(res.cities||[]).filter(c => c.is_active)`. Net effect: a Super Admin adds 'Sierra Leone' here, it saves 'successfully', shows status 'Inactive' in the table, and silently does NOT appear in the school registration geography dropdowns until the SA separately clicks the toggle on each row.
IMPACT: The stated purpose of this page ('Used to populate geography dropdowns during registration', SuperadminDashboard.js:1325) is broken on the happy path. Operators add geography and it never reaches registrants; the failure is silent (no error, no hint).
FIX: Set is_active: true on create in createCountry/createRegion/createCity (or change the model default to true), so added geography is live by default. Alternatively keep default-off but surface it in the UI ('Inactive rows are hidden from registration — activate to publish').
VERIFY-NOTE: Finding is accurate. One refinement to the impact: "silently does NOT appear ... never reaches registrants" is literally true for COUNTRIES (no fallback path), but for REGIONS and CITIES Register.js falls back to the public geo API (fetchStates/fetchCities) when the curated active list is empty, so a brand-new country's regions/cities still populate from the public API rather than rendering blank. A newly added inactive region/city for a country that already has other active curated regions is still hidden (fallback won't trigger because the list isn't empty). The new curated row is hidden from registration until the SA toggles it in all three cases.

### [21] MEDIUM · confirmed · area=geography · cat=backend-bug
TITLE: Deleting a country/region hard-deletes the row and orphans all child regions/cities with no FK guard or force flow
PAGE: countries / regions  | COMPONENT: EK_SMS/src/components/superadmin/SARefDataManager.js (handleDelete L467-485)  | BACKEND: DELETE /api/countries/:id, DELETE /api/regions/:id -> deleteCountry/deleteRegion
EVIDENCE: deleteCountry (superadminDataController.js:1967-1978) and deleteRegion (:2052-2063) do an unconditional hard `await row.destroy()` with no check for dependent rows. Region.js / City.js declare country_id/region_id as plain BIGINT columns with NO Sequelize associations or DB-level cascade, so deleting a country leaves its regions and cities pointing at a non-existent country_id (same for region->cities). The frontend handleDelete (SARefDataManager.js:472) even supports a `?force=1` + 409 `requiresForce` confirm flow, but the geography controllers never emit 409/requiresForce, so the user gets no 'this country has N regions/cities' warning — one click silently orphans the whole subtree.
IMPACT: Data integrity loss: orphaned regions/cities remain in pruh_system_region/pruh_system_city referencing deleted parents; cities can outlive their region/country. The cities cascade in the form (region filtered by country_id) then shows regions whose country no longer exists, or cities under a deleted region.
FIX: Before destroy, count children (Region.count({where:{country_id:id}}), City.count({where:{region_id:id}} / {country_id:id}}); if >0 return 409 with requiresForce + the count (matching the shell's existing force flow), and on force either block, cascade-delete, or deactivate. At minimum block deletion of a country/region that still has children.

### [22] MEDIUM · confirmed · area=masterdata-catalog · cat=workflow-gap
TITLE: School registration discards SA-managed institution/academic/grading master data and uses hardcoded constants
PAGE: academic-system / grading-system / institution-type  | COMPONENT: EK_SMS/src/components/Register.js  | BACKEND: GET /api/institution-types|academic-systems|grading-systems → getInstitutionTypes/getAcademicSystems/getGradingSystems (public, n/a controller bug)
EVIDENCE: Register.js:1389-1393 fetches all three endpoints but the extract callbacks are no-ops: `fetchOne('/api/institution-types/', () => { /* options managed client-side */ })`, same for academic-systems and grading-systems. The dropdowns are instead driven by hardcoded constants — Register.js:291 `DEFAULT_INSTITUTION_TYPES`, :310 `DEFAULT_ACADEMIC_SYSTEMS`, :320 `DEFAULT_GRADING_SYSTEMS` — bound via read-only state with no setter: Register.js:1233 `const [institutionTypes] = useState(DEFAULT_INSTITUTION_TYPES);`, :1239, :1240 (single-element destructure, so the fetch can never replace them). Rendered at :1945, :2261, :2267. The whole point of the SA Master Data CRUD for these taxonomies is to feed the registration form; that wiring is dead.
IMPACT: A super admin adding/editing/removing an Institution Type, Academic System, or Grading System sees their change persist in the catalog but it never appears in (or is removed from) the school self-registration form. The CRUD UI looks functional but has no effect on the canonical onboarding workflow it exists to drive.
FIX: Replace the no-op extract callbacks with real setters that map the API rows (filtering on is_active) into the dropdown state, and add the missing state setters (`const [institutionTypes, setInstitutionTypes] = useState(...)`). Fall back to the DEFAULT_* constants only when the API returns an empty list.
VERIFY-NOTE: The finding is accurate as stated. One refinement: this is an intentional design decision (explicit code comments, and curated backend data IS wired for countries/regions), not broken code — and the root cause that blocks a trivial fix is a schema mismatch: the SA CRUD persists only a free-text `name`, while the academic-system and grading-system dropdowns must emit stable coded enum values that downstream logic relies on. Fixing the wiring requires the master-data rows to carry a `value`/`code` column (and a curated `is_active`-driven list), not just pointing the existing no-op callbacks at r.types / r.academicsystems / r.gradingsystems.

### [23] MEDIUM · confirmed · area=masterdata-catalog · cat=backend-bug
TITLE: Catalog create/update handlers have no duplicate-name or non-empty validation (unlike Academic Years)
PAGE: institution-type / school-type / syllabus-type / class-subtype / academic-system / grading-system / capacity-category  | COMPONENT: n/a  | BACKEND: createInstitutionType / createSchoolType / createSyllabusType / createClassSubtype / createAcademicSystem / createGradingSystem / createCapacityCategory
EVIDENCE: Every taxonomy create only checks truthiness, then inserts: superadminDataController.js:1556 `if (!name) return ...; const row = await InstitutionType.create({ name: String(name).slice(0,100) })`; identical at :2194 (school type), :2262 (syllabus), :2326 (class subtype), :2546 (academic system), :2591 (grading system), :1781 (capacity category). No trim (so a whitespace-only name like ' ' passes `if (!name)`), no case-insensitive duplicate check, no per-table unique constraint in the models. Contrast with validateYearPayload (superadminDataController.js:788-835) which trims, length-caps and de-dupes. updateX handlers (:1571, :2209, :2277, :2336, :2556, :2601) are likewise unvalidated.
IMPACT: Super admin can create duplicate 'Government' school types or blank-named rows, polluting the catalog and the registration dropdowns with ambiguous/empty options. No server-side guardrail.
FIX: Add a shared validator for single-name taxonomies: trim + reject empty, length-cap, and case-insensitive duplicate check excluding self on update; return 400 with fieldErrors so SARefDataManager surfaces it inline (it already handles err.payload.fieldErrors at SARefDataManager.js:429).

### [24] MEDIUM · confirmed · area=masterdata-catalog · cat=backend-bug
TITLE: Deleting a Capacity Category that is referenced by School Capacities is not blocked (orphan/FK risk)
PAGE: school-capacity  | COMPONENT: EK_SMS/src/components/superadmin/SASchoolCapacity.js  | BACKEND: DELETE /api/capacity-categories/:id/ → deleteCapacityCategory
EVIDENCE: deleteCapacityCategory (superadminDataController.js:1805-1816) does `await row.destroy()` with no check for dependent SchoolCapacity rows that reference it via capacity_category_id. getSchoolCapacities (:1840) then looks up `CapacityCategory.findByPk(r.capacity_category_id)` and on a missing parent returns capacity_category_name=null, which the UI renders as the em-dash placeholder (SARefDataManager displayVal). createSchoolCapacity correctly validates the parent exists (:1864), but nothing protects it from later deletion.
IMPACT: A super admin can delete a capacity category still used by School Capacities, leaving orphaned rows that display a blank category name. The two-tab SASchoolCapacity workflow (define categories first, then amounts) is undermined because step-1 data can vanish out from under step-2.
FIX: In deleteCapacityCategory, count SchoolCapacity rows with that capacity_category_id; if >0 return a 409 (the frontend already understands 409+requiresForce, SARefDataManager.js:477) or a plain 400 explaining it is in use. Optionally add an ON DELETE RESTRICT FK.

### [25] MEDIUM · confirmed · area=people-access · cat=backend-bug
TITLE: User list role column is derived heuristically and mislabels teachers/bursars/principals as plain 'User'
PAGE: users  | COMPONENT: EK_SMS/src/components/superadmin/SAUsers.js  | BACKEND: GET /api/users/ -> superadminDataController.getUsers / mapUserToSaRow
EVIDENCE: mapUserToSaRow ignores the user's real role_id and infers role only from `is_superuser` / `is_staff` / presence of a SchoolAdmin link: `let role='User'; if (user.is_superuser) role='Super Admin'; else if (user.is_staff) role='Staff Admin'; else if (schoolName) role='School Admin';` (superadminDataController.js:290-293). Teachers, bursars, principals, parents and students all fall through to role='User'. getUsers only joins SchoolAdmin (superadminDataController.js:320), never Teacher/Bursar/etc.
IMPACT: The User Management role filter and role badges are wrong for most tenant users — a teacher shows as 'User', defeating the role filter (SAUsers.js:591-597) and the Governance per-role user counts which key off this same field. Operators can't reliably find users by role.
FIX: Join the users.role_id -> roles.code and map to the display label in mapUserToSaRow instead of guessing from is_staff/SchoolAdmin presence, so the role reflects the actual seeded role.
VERIFY-NOTE: The finding mis-states which roles get mislabeled. It claims teachers, bursars, and principals fall through to role='User'. They do NOT. The User.is_staff virtual (User.js:36-43) returns true for superadmin, schooladmin, principal, bursar, AND teacher, and the is_staff branch (line 292) is evaluated before the schoolName branch (line 293). So teachers, bursars, principals, and even schooladmins all map to 'Staff Admin' (not 'User', and schooladmins not even 'School Admin'). Only parents and students (is_staff=false, no SchoolAdmin link) actually fall through to 'User'. The net effect the finding describes — wrong role labels, a broken role filter, and wrong Governance per-role counts — is accurate; only the specific 'User' bucket attribution for teacher/bursar/principal is wrong (they collapse into 'Staff Admin' instead). Either way the real role is lost.

### [26] MEDIUM · confirmed · area=people-access · cat=data-mismatch
TITLE: Governance shows a hardcoded 'Registrar' role that does not exist in the backend
PAGE: governance  | COMPONENT: EK_SMS/src/components/superadmin/SAGovernance.js  | BACKEND: n/a (no role source) — real custom roles come from GET /api/sa/custom-roles/
EVIDENCE: ROLE_DEFS hardcodes `{ id:'registrar', name:'Registrar', ... isCustom:true, apiRole:'Registrar' }` (SAGovernance.js:11) and it is always merged into the role list (SAGovernance.js:340). But seedRoles.js:13-21 has no registrar role and mapInviteLabelToCode has no Registrar. Its user count is `roleCounts['Registrar'] || 0` (SAGovernance.js:350 logic) so it permanently shows 0 users. Real custom roles created via the modal are stored separately in settings.custom_roles (postSaCustomRoles, superadminDataController.js:718-731).
IMPACT: Operators see a 'Registrar / Custom Role' card that is decorative — it maps to no real role, can never have users, and editing its permissions writes a settings key (`registrar`) that authorization never reads. Misleading governance surface.
FIX: Drop the fabricated Registrar entry from ROLE_DEFS (and the principal/teacher entries should be driven by the seeded roles list), and render only the four seeded non-superadmin roles plus genuine custom_roles from the API.
VERIFY-NOTE: The finding is correct. Minor clarifications: (1) The exact merge line is the `ROLE_DEFS.map(...)` at SAGovernance.js:340-342 (the cited "line 340" is right). (2) The user-count expression resolves via `roleCounts[r.apiRole] || 0` (line 341) where r.apiRole='Registrar'; mapUserToSaRow can only emit 'User'/'Super Admin'/'Staff Admin'/'School Admin', so the count is guaranteed 0 — even more strongly than stated (it also can never match the Teacher/Principal base roles). (3) The page already carries a global 'Preview — not enforced' honesty banner (lines 437-442) that discloses the matrix is non-authoritative, so the 'writes a settings key authorization never reads' sub-impact is partly acknowledged; but the registrar role itself is not flagged as fabricated, so the misleading-role defect remains real.

### [27] MEDIUM · confirmed · area=people-access · cat=backend-missing
TITLE: Permission matrix is fully editable/saved but never enforced (config-only); custom-role permissions are silently discarded
PAGE: governance  | COMPONENT: EK_SMS/src/components/superadmin/SAGovernance.js  | BACKEND: PATCH /api/admin-settings/ (rbac_role_permissions) ; POST /api/sa/custom-roles/ -> postSaCustomRoles
EVIDENCE: handleSave persists the whole perms object to settings via `ApiClient.patch('/api/admin-settings/', { settings: { rbac_role_permissions: perms } })` (SAGovernance.js:380) but nothing in the auth path reads rbac_role_permissions. The page itself admits this with the 'Preview — not enforced' banner (SAGovernance.js:437-442). Separately, CreateRoleModal POSTs `permissions: {}` (SAGovernance.js:226) and postSaCustomRoles only persists `{ id, name, description }` (superadminDataController.js:724-728) — any permissions assigned to a custom role are dropped on save.
IMPACT: The core governance promise (RBAC) is non-functional: toggling permissions changes nothing about access, and custom roles cannot carry permissions at all. The banner is honest, but this is a real workflow gap for the 'manage governance' SA responsibility.
FIX: Either wire rbac_role_permissions into the requireRole/authorization middleware, or scope the page to clearly 'coming soon'. Extend postSaCustomRoles to persist a permissions object and have the editor save custom-role perms under the same key.
VERIFY-NOTE: Finding is accurate as written. One refinement to add: the honesty banner (lines 436-442) covers ONLY the permission matrix; it does not disclose the custom-role permissions drop. CreateRoleModal's own copy says 'Add a new role you can later assign permissions to' (line 252) and it POSTs a permissions field (line 226), implying permissions can be carried — but the backend has no field for them, so that promise is silently unmet and undisclosed. Also note that base-role permissions are persisted under settings.rbac_role_permissions while custom roles are stored separately under settings.custom_roles (without any permissions key), so even the data models for the two are disconnected from each other and from the auth layer.

### [28] MEDIUM · confirmed · area=reports-analytics · cat=backend-missing
TITLE: getGradeStats never returns `passed` or `distribution`, so Pass Rate KPI and Grade Distribution chart are permanently dead even with grades in the DB
PAGE: benchmarks  | COMPONENT: EK_SMS/src/components/superadmin/SABenchmarks.js  | BACKEND: GET /api/grade-stats/ -> superadminDataController.getGradeStats
EVIDENCE: SABenchmarks.js:211 computes passRate only `if (totalGrades > 0 && gradeStats?.passed != null)`, and line 215 builds the distribution from `gradeStats?.distribution`. getGradeStats (superadminDataController.js:475-486) returns schools, grade_events_30d, integrity_score, pending_reviews, total_grades, locked_grades, unlocked_grades, rejected_grades, average_score, per_school — there is NO `passed` and NO `distribution` field. So passRatePct is always null (Pass Rate KPI shows '—' / 'No data', 272) and gradeDist is always [] (Grade Distribution shows 'No grade data available yet.', 295) even when the DB has thousands of grades.
IMPACT: Two of the four headline academic-quality outputs on Benchmarks (Pass Rate and the Grade Distribution bar chart) can never render real numbers because the endpoint omits the fields the UI needs. The frontend comments acknowledge this, but the user-facing result is a chart that is structurally impossible to populate.
FIX: Extend getGradeStats to return `passed` (e.g. COUNT where total >= pass threshold) and `distribution` (COUNT grouped by letter grade A/B/C/D/E). The per-school AVG machinery is already in the controller, so add a grade-band aggregate alongside it.

### [29] MEDIUM · confirmed · area=reports-analytics · cat=ui-not-functional
TITLE: Benchmarks year/term/grade filters are decorative — hardcoded fake options that filter nothing
PAGE: benchmarks  | COMPONENT: EK_SMS/src/components/superadmin/SABenchmarks.js  | BACKEND: n/a
EVIDENCE: SABenchmarks.js:258-260 hardcodes options `['2023-24','2024-25','2022-23']`, `['All Terms','Spring','Fall','Summer']`, `['K-12','Primary','Secondary','Tertiary']`. `term` and `grade` state are written but never read after declaration (grep shows no further use); only `year` is read, purely as a display label at line 351. The data useEffect (191-203) has empty deps `[]` and never re-fetches when any filter changes. The grade-stats/school-stats endpoints also take no year/term/grade params.
IMPACT: An operator who picks a year/term/grade expects the numbers to change; they never do. 'Spring/Fall/Summer' are US seasons, not this platform's term model (the system uses SystemAcademicYear/SystemTerm). The filter bar is a misleading no-op that implies time-sliced analytics the backend can't provide.
FIX: Either remove the filter bar until the endpoints support period scoping, or populate the dropdowns from /api/academic-years/ + /api/system-terms/ and pass the selected period to the stats endpoints so the data actually re-fetches.
VERIFY-NOTE: The filters are non-functional as stated, with one precision: `year` is not entirely unused after declaration — it renders as the Platform Summary card meta label at line 351 (<span className="san-card-meta">{year}</span>). It still drives no data fetch or filtering. `term` and `grade` are genuinely write-only (read nowhere except their own select). The net effect — picking any year/term/grade changes nothing in the numbers — is exactly as the finding describes, because the single useEffect has empty deps and the grade-stats/school-stats endpoints accept no year/term/grade params.

### [30] MEDIUM · confirmed · area=schools-onboarding · cat=backend-bug
TITLE: approval_date is fabricated as registration_date in the serializer
PAGE: applications  | COMPONENT: EK_SMS/src/components/superadmin/SAApplications.js  | BACKEND: GET /api/schools/ -> getAllSchools / serializeSchool
EVIDENCE: superadminController.js:35 `const regDate = p.created_at ? new Date(p.created_at).toISOString() : null;` then line 66 `approval_date: approved ? regDate : null`. There is no real approved-at column; approval_date is just the registration date for any approved school. SAApplications.js:79-88 computes avgReview as the mean of `(new Date(s.approval_date) - new Date(s.registration_date))` — which is always exactly 0 because the two values are identical, so 'Avg Review' always renders '0m' (or '—'). SAReview.js:373-377 also shows an 'Approved' date that is really the registration date.
IMPACT: The 'Avg Review' KPI on the Onboarding page is meaningless (always 0m), and the 'Approved' date shown on the review screen is wrong (it shows when the school registered, not when it was approved). Operator sees fabricated review-time metrics.
FIX: Add a real `approved_at` (and ideally `rejected_at` / `changes_requested_at`) timestamp column, set it in handleSchoolAction on the approve branch, and serialize the true value. Until then, do not derive approval_date from created_at.
VERIFY-NOTE: Finding is accurate. Minor precision: 'Avg Review' renders '—' only when no approved school has both dates; for any approved school it is always exactly '0m', never a meaningful value. Also worth noting the same stat card hardcodes a bogus '−10m faster' trend label (SAApplications.js:94), reinforcing that the review-time metric is entirely fabricated. Root cause for the fix: add a real approved_at (and ideally reviewed_at) timestamp column to pruh_core_school, set it in handleSchoolAction's approve branch, and serialize that instead of created_at.

### [31] MEDIUM · confirmed · area=shell-nav · cat=ui-bug
TITLE: Sidebar Notifications badge + header bell dot never reflect unread notifications until the operator first opens the Notifications page
PAGE: notifications / topbar bell  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: n/a (no unread-count fetch on shell mount)
EVIDENCE: unreadNotifCount starts at 0 (SuperadminDashboard.js:456) and is ONLY updated via onUnreadChange={setUnreadNotifCount} passed to <SANotifications> (line 1264), which renders only when activePage==='notifications'. There is no fetch of the unread count when the shell mounts. The sidebar badge uses badge: unreadNotifCount (line 959) and the header dot shows when (pendingCount>0 || unreadNotifCount>0) (line 1098). So on login the Notifications badge is always 0 and the bell dot is driven solely by pendingCount until the user navigates into Notifications once.
IMPACT: Operators get no unread-notification signal in the nav/header until they happen to open the Notifications page, defeating the purpose of the badge/dot. New alerts are invisible at a glance.
FIX: Fetch the unread notification count once on shell mount (and on an interval / after actions) into unreadNotifCount, independent of whether the Notifications page is mounted — mirror how pendingCount/gradeAlerts are fetched at init.

### [32] MEDIUM · confirmed · area=years-terms · cat=workflow-gap
TITLE: System-term edit / delete / toggle endpoints have NO UI — terms can be created and rolled out but never modified or removed
PAGE: academic-terms  | COMPONENT: EK_SMS/src/components/superadmin/SACreateTerm.js  | BACKEND: PUT /api/system-terms/:id/ (updateSystemTerm), DELETE /api/system-terms/:id/ (deleteSystemTerm), PATCH /api/system-terms/:id/toggle/ (toggleSystemTermStatus) — superadminDataController.js:1464/1487/1500
EVIDENCE: SACreateTerm.js only calls POST /api/system-terms/ (create, :238) and POST /api/system-terms/:id/rollout/ (:184, :241). A grep across EK_SMS/src for system-terms shows only those two rollout calls and the create — no apiPut/apiDelete/toggle to system-terms anywhere. The existing-terms list (SACreateTerm.js:359-386) renders each term with only a 'Roll Out' button / 'Active' badge; there is no edit, rename, date-fix, deactivate, or delete control. The three backend handlers (updateSystemTerm, deleteSystemTerm, toggleSystemTermStatus) are wired in routes (superadmin.js:147-149) but unreachable.
IMPACT: A superadmin who fat-fingers a term name/date, or creates a wrong/duplicate term, has no way to fix or remove it from the console. Terms accumulate with no lifecycle management. Three implemented endpoints are dead weight.
FIX: Add edit (inline form), deactivate (toggle), and delete controls to each row in SACreateTerm.js's existing-terms list, wiring to PUT/DELETE/PATCH /api/system-terms/:id/. Mirror the SARefDataManager row-action pattern.
VERIFY-NOTE: Finding is accurate; only the severity is overstated. It should be medium, not high — the three endpoints exist and are routed (superadmin.js:147-149) but have no UI caller, and the core create/rollout flow still functions. The remaining real impact is the absence of any edit/delete/deactivate control, leaving accumulating cruft and no clean removal of a mistyped active term.

### [33] MEDIUM · confirmed · area=years-terms · cat=backend-bug
TITLE: createSystemTerm performs no date validation — accepts end<start, out-of-year-range, and overlapping terms; frontend only warns
PAGE: academic-terms  | COMPONENT: EK_SMS/src/components/superadmin/SACreateTerm.js  | BACKEND: POST /api/system-terms/ -> createSystemTerm (superadminDataController.js:1437)
EVIDENCE: createSystemTerm (:1437-1462) only checks `system_academic_year_id` and `name` are present (:1440) and that the year exists (:1441-1442); it writes start_date/end_date straight through with no start<end check, no within-year-bounds check, and no overlap check — unlike validateYearPayload for years (:809-832 which does all three). The frontend computes `overlapWith` (SACreateTerm.js:160), `outOfRange` (:161) and shows warning banners (:509-532), but `isValid` (:164-168) only blocks on missing year/order/name and `endBeforeStart`; overlap and out-of-range are non-blocking warnings. So a user can ignore the warnings and the server happily persists an invalid term.
IMPACT: Garbage term date ranges (overlapping, reversed where end==start, or outside the academic year) can be saved, which then cascade to every school on rollout and feed timetable scheduling, corrupting the calendar.
FIX: Add server-side validation in createSystemTerm/updateSystemTerm: reject end_date <= start_date, reject dates outside the parent year's bounds, and warn/reject on overlap with sibling terms. Return fieldErrors so the existing form error plumbing surfaces them.
VERIFY-NOTE: One detail is mis-stated: the finding says invalid terms with "reversed where end==start" can be saved via the UI. The frontend DOES block end<=start through endBeforeStart (SACreateTerm.js:162, `new Date(endDate) <= new Date(startDate)`), so end==start and end<start are blocked client-side. The true defect is that the BACKEND has no such guard at all — a non-browser client (curl/Postman) can POST end<start, end==start, overlapping, and out-of-range dates, and the server persists them. So the overlap and out-of-range cases are bypassable through the UI (non-blocking warnings), while the reversed-date case is only reachable by hitting the API directly. The backend validation gap is the actual root cause, and it also affects updateSystemTerm, not just create.

### [34] MEDIUM · confirmed · area=years-terms · cat=backend-bug
TITLE: Rollout cascade matches per-school years/terms by case-insensitive name only — silent duplicates / wrong-row activation on any name drift
PAGE: academic-year  | COMPONENT: EK_SMS/src/components/superadmin/SARefDataManager.js  | BACKEND: POST /api/academic-years/:id/rollout/ (cascade=true) -> rolloutAcademicYear (superadminDataController.js:1058)
EVIDENCE: The cascade joins system years/terms to per-school rows purely by LOWER(name): AcademicYear.findOrCreate keyed on `LOWER(name)=row.name` (:1092-1098) and Term.findOrCreate keyed on `LOWER(name)=st.name` (:1121-1128). There is no stable foreign key (e.g. system_academic_year_id) on pruh_core_academicyear/pruh_core_term linking back to the system record. If a school already created its own '2024/2025' year by hand, the system rollout silently adopts that row and flips it active. If a system term was renamed (e.g. SACreateTerm prefixes names as `1st Term — X`, SACreateTerm.js:231-233) after an earlier rollout, the next rollout creates a duplicate per-school term instead of updating. getRolloutPreview/getAcademicYearAdoption use the same fragile LOWER(name) match (:1184, :1328), so the 'already have it' / adoption counts drift too.
IMPACT: Cascade rollout can activate a school's pre-existing same-named (possibly differently-dated) year, or spawn duplicate terms, and the adoption/preview metrics misreport coverage. Hard-to-trace calendar drift across tenants.
FIX: Add a nullable `system_academic_year_id` FK to pruh_core_academicyear (and `system_term_id` to pruh_core_term), match on that in findOrCreate and in preview/adoption, and fall back to name only for legacy rows.
VERIFY-NOTE: Finding is accurate as stated. Minor refinements: the preview/adoption LOWER(name) queries are global (not tenant-scoped beyond grouping by school_id), and getAcademicYearAdoption's matchMap is keyed by school_id so a school with two same-named years only surfaces one — secondary effects of the same root cause. Also note the cascade's wrong-row activation deactivates ALL of that school's other academic years (lines 1111-1114), which amplifies the calendar-drift impact when the adopted row is the wrong (e.g. differently-dated) one.

### [35] MEDIUM · unverified · area=grade-integrity · cat=security
TITLE: /api/grade-alerts/ is a shared route, not superadmin-gated
PAGE: grade-integrity  | COMPONENT: n/a  | BACKEND: router.get('/grade-alerts/', getGradeAlerts) (routes/superadmin.js:100)
EVIDENCE: grade-alerts is registered at routes/superadmin.js:100, BEFORE the superadmin-only sub-router `sa` (created at line 111 with `sa.use(requireRole(['superadmin']))`). It sits under the shared block (after authenticateToken + schoolScope at lines 95-96) alongside /dashboard/ and /profile/. So any authenticated user - including a school_admin holding an impersonation token, or any tenant role - can call /api/grade-alerts/ and read cross-school grade-modification alerts. getGradeAlerts also does NO tenant scoping: it findAll's all grade alerts platform-wide (limit 200) with no school_id filter.
IMPACT: Cross-tenant grade-integrity alert data (student names, schools, change reasons) is readable by non-superadmin roles. Low live impact today only because the table is empty, but it becomes a cross-tenant PII leak the moment the source is wired up.
FIX: Move the grade-alerts route under the `sa` sub-router (after line 112) so requireRole(['superadmin']) applies, or add explicit superadmin + tenant scoping inside getGradeAlerts before the source is populated.

### [36] MEDIUM · unverified · area=grade-integrity · cat=responsiveness
TITLE: SAGradeReport has zero @media rules and a hardcoded 2-column risk grid that breaks on mobile
PAGE: grade-report  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeReport.js:444  | BACKEND: n/a
EVIDENCE: SAGradeReport is entirely inline-styled with no @media handling. The Risk Analysis grid is fixed `gridTemplateColumns: '1fr 1fr'` (line 444) with className `sa-gi-risk-grid` - the className implies a CSS override exists, but the file imports no CSS and SAGradesAccumulation.css (the only css read in this bucket) has no `.sa-gi-risk-grid` rule. So on a <=600px screen the donut card and the schools-at-risk card stay side-by-side at half width, squashing the donut and bar labels. The chart wrapper is `height:160` with a 400-wide SVG viewBox (lines 379,387) and the recent-logs table relies on the inline `overflowX:auto` wrapper (line 511) only.
IMPACT: On phones the grade-report risk cards are cramped/overlapping and the donut legend wraps badly, violating the mandatory mobile-responsiveness project rule (<=600px rules, >=44px touch targets).
FIX: Add a stylesheet (or extend an existing SA css) with `@media (max-width:600px){ .sa-gi-risk-grid{ grid-template-columns:1fr; } }` and verify the period toggle / 'View All' buttons meet 44px touch targets. Confirm where `.sa-gi-risk-grid`, `.sa-gi-donut`, `.sa-gi-bar-row`, `.sa-progress-*` are actually defined.

### [37] MEDIUM · unverified · area=notifications · cat=ui-bug
TITLE: Sidebar bell badge stays 0 until the user opens Notifications; unread count never loads globally
PAGE: notifications  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: n/a
EVIDENCE: unreadNotifCount is initialised to 0 (SuperadminDashboard.js:456) and is ONLY ever updated via onUnreadChange={setUnreadNotifCount} passed to <SANotifications> (line 1264). SANotifications is lazy and only mounts when activePage==='notifications' (line 1259-1267). The sidebar Notifications row renders badge: unreadNotifCount (line 959), so before the operator ever visits the page the badge shows 0 even when there are genuinely-unread items (pending applications, critical security events). Inside SANotifications the report effect is further gated by `if (notifications.length > 0)` (SANotifications.js:243), so an all-empty mount never even resets it.
IMPACT: The unread badge on the primary nav is useless for at-a-glance monitoring — it only becomes accurate after the operator manually opens the page, which is the opposite of what a notification badge is for.
FIX: Compute the unread count at the shell level from data already loaded globally (schools + gradeAlerts, plus a lightweight security-logs/admin-settings fetch) and seed unreadNotifCount on mount, rather than depending on the lazy page mounting. The header dot already partly compensates by OR-ing pendingCount (line 1098), but the numeric sidebar badge does not.

### [38] MEDIUM · unverified · area=notifications · cat=workflow-gap
TITLE: Mark-as-read / Mark-all / Dismiss are session-only — no backend, reset on reload
PAGE: notifications  | COMPONENT: EK_SMS/src/components/superadmin/SANotifications.js  | BACKEND: n/a (no read-state endpoint exists)
EVIDENCE: markRead (SANotifications.js:250-256), markAllRead (258-261) and dismiss (263-269) mutate only local React state — none call ApiClient. The whole feed is re-derived from props/fetches on every mount via buildNotifications (line 225-228), and read-state is only preserved within the live session by the readSet merge (line 234-239). There is no notifications table / no PATCH endpoint, so after a page refresh every dismissed item reappears and every 'mark read' is forgotten (read-defaults are recomputed from is_approved/status each time, lines 33,42,51,60,83,109).
IMPACT: An operator who triages the queue (reads/dismisses items) sees the entire backlog return on the next login. 'Mark all as read' gives a false sense of a cleared inbox. Dismiss is purely cosmetic.
FIX: Persist read/dismissed state — minimally per-user in localStorage keyed by notification id, or properly via a backend read-receipts endpoint. At least scope the localStorage approach the way SAProfile does (ek-sms-... per user.id) so a shared browser doesn't leak state across operators.

### [39] MEDIUM · unverified · area=rejected · cat=workflow-gap
TITLE: Reconsider sends a hardcoded internal note as the change-request reason; operator cannot say what to change
PAGE: rejected  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: POST /api/schools/approve/ -> handleSchoolAction (request_changes), superadminController.js:203-218
EVIDENCE: SuperadminDashboard.js:575 hardcodes the note: `handleAction(school.id, 'request_changes', 'Reconsidering rejected application')`. The backend uses that note verbatim: the change-request email body is `note || 'Please review and update your application.'` (superadminController.js:218), so the school receives the literal internal phrase 'Reconsidering rejected application' as guidance, AND `school_changes_requested` audit metadata stores note='Reconsidering rejected application' (superadminController.js:232). The reconsider flow gives the operator no field to tell the school what was wrong, even though the whole point is to return it for fixes.
IMPACT: The school is asked to 'update the application' but gets a meaningless internal string as the reason. The audit log records the same uninformative note. Operators cannot explain what to fix on reconsider.
FIX: Capture a real note in the Reconsider confirm dialog and pass it as the action note so both the email body and the audit metadata carry the operator's guidance.

### [40] MEDIUM · unverified · area=rejected · cat=ui-not-functional
TITLE: Fraud / Incomplete / Policy filter tabs almost never match because rejection reason is free-text and optional
PAGE: rejected  | COMPONENT: EK_SMS/src/components/superadmin/SARejected.js  | BACKEND: handleSchoolAction reject branch superadminController.js:151 (rejection_reason = note || 'Rejected by superadmin')
EVIDENCE: SARejected.js:47-52 defines tabs Fraud Risk / Incomplete Docs / Policy Violation; SARejected.js:79-81 filters by substring on the free-text reason: `(s.rejection_reason||'').toLowerCase().includes('fraud')`, `.includes('incomplete') || .includes('document')`, `.includes('policy')`. But the reject reason is a free-text OPTIONAL field — SAReview.js:103 placeholder 'Reason for rejection (optional)…', and the backend defaults an empty reason to the literal 'Rejected by superadmin' (superadminController.js:151). There is no structured reason category anywhere. So unless an operator happens to type the exact words fraud/incomplete/document/policy, every category tab shows zero results, and the common default reason matches none of them.
IMPACT: Three of the four filter tabs are effectively dead UI that almost always render an empty list, making the operator think there are no fraud/incomplete/policy rejections when really the reason text just didn't contain those keywords.
FIX: Introduce a structured rejection-reason category (select in the reject modal) persisted alongside the free-text note, and filter the tabs on that category instead of substring-matching prose.

### [41] MEDIUM · unverified · area=rejected · cat=data-mismatch
TITLE: Rejection audit fabricates a fake event timeline when the live log is empty, presented as the record
PAGE: rejection-audit  | COMPONENT: EK_SMS/src/components/superadmin/SARejectionAudit.js  | BACKEND: GET /api/security-logs/ -> getSecurityLogs (superadminDataController.js:133-163)
EVIDENCE: SARejectionAudit.js:144-149 builds `fallbackEvents` that invent steps — 'Super Admin opened the application for manual review' (f-review) and 'Rejection Decision Made' with actor 'Super Admin' — and SARejectionAudit.js:148 sets ALL fallback timestamps to `school.registration_date`, so the fabricated rejection appears to have happened at submission time. Likewise rejectedBy/rejectedTs default to 'Super Admin' / registration_date (SARejectionAudit.js:141-142) when no real event matches. realEvents only match if a security log has `metadata.school_id === school.id` (SARejectionAudit.js:123); any school rejected before security-audit logging existed, or whose log row rolled past the limit=500 window, silently shows this fabricated trail. The 'Reconstructed' badge (line 178) is the only hint, easy to miss.
IMPACT: On the audit screen — the page whose entire job is a trustworthy record — a school with no real audit rows shows invented reviewer actions and a wrong rejection timestamp equal to the submission date. An operator could cite fabricated 'facts' from the audit trail.
FIX: When no real audit events exist, show an explicit empty/unavailable state ('No audit events recorded for this school') instead of synthesizing reviewer steps and a fake rejection time; never present invented actors/timestamps in an audit view.

### [42] MEDIUM · unverified · area=rejected · cat=backend-bug
TITLE: Audit trail can miss the rejection event because security-logs is capped at 500 rows with no school filter
PAGE: rejection-audit  | COMPONENT: EK_SMS/src/components/superadmin/SARejectionAudit.js  | BACKEND: GET /api/security-logs/ -> getSecurityLogs (superadminDataController.js:133-158)
EVIDENCE: SARejectionAudit.js:106 fetches `/api/security-logs/?limit=500` then filters client-side by school_id (line 123). getSecurityLogs (superadminDataController.js:135) hard-caps `limit` at `Math.min(parsed, 500)` and orders by ts DESC with no per-school query param. There is no endpoint to fetch a single school's audit events; the component pulls the 500 most-recent platform-wide events and hopes the target school's rejection is among them. On an active platform the school_registration_submitted / school_rejected rows for an older rejected school will fall outside the newest 500 and the timeline silently degrades to the fabricated fallback.
IMPACT: For any school whose audit rows are older than the most recent 500 platform events, the 'Live' audit trail can never load — it always reconstructs. The audit page does not scale and gives no indication the data was truncated.
FIX: Add a scoped backend query (e.g. GET /api/security-logs/?school_id=) that returns all events for one school, and have SARejectionAudit call that instead of fetching+filtering the global 500-row window.

### [43] MEDIUM · unverified · area=rejected · cat=backend-bug
TITLE: getAllSchools returns every school with admins+users on each render, no pagination or filter
PAGE: rejected  | COMPONENT: EK_SMS/src/components/superadmin/SARejected.js (consumer)  | BACKEND: GET /api/schools/ -> getAllSchools (superadminController.js:75-91)
EVIDENCE: getAllSchools (superadminController.js:77-84) does `School.findAll` with an include of SchoolAdmin -> User and NO limit/where, then serializes all of them. SARejected (and the badge formula in SuperadminDashboard.js:664) receives the entire `schools` array and filters client-side (`schools.filter(s => !s.is_approved && !s.is_active)`, SARejected.js:59). The whole onboarding/rejected/schools surface depends on this one unbounded query; there is no server-side filter for the rejected bucket and no pagination.
IMPACT: As the platform grows the Rejected page (and its badge count, and Onboarding/Schools) load every school with joined admin+user rows on each fetch, growing payload and DB cost linearly. No way to page through a large rejected archive.
FIX: Add server-side filtering (e.g. ?status=rejected) and pagination to getAllSchools, and have the Rejected page request only the rejected slice rather than the full table.

### [44] MEDIUM · unverified · area=settings-profile · cat=data-mismatch
TITLE: Super-admin profile prefs (phone, bio, avatarColor, language, timezone) are saved into the single shared global settings row
PAGE: profile  | COMPONENT: EK_SMS/src/components/superadmin/SAProfile.js  | BACKEND: PATCH /api/admin-settings/ -> patchAdminSettings
EVIDENCE: SAProfile.js:205 saves `{ phone, bio, language, timezone, avatarColor }` via `ApiClient.patch('/api/admin-settings/', { settings: {...} })`, and reads them back from the same endpoint on mount (lines 147-156). admin-settings is the ONE global row id:1 (loadSettings:111-123) shared with platform-wide config. getProfile (superadminDataController.js:192-208) returns only full_name/email/username/date_joined and never phone/bio. Combined with the un-gated route above, any authenticated user can read the super admin's phone/bio out of /admin-settings/.
IMPACT: Personal profile fields live in platform config rather than on the user record; works only because there's exactly one super admin, and leaks PII given the missing role gate. A second platform operator would overwrite the first's phone/bio.
FIX: Persist phone/bio/avatarColor/language/timezone on the User record (or a per-user profile table) via /profile/, not the global admin-settings JSON; return them from getProfile.

### [45] MEDIUM · unverified · area=settings-profile · cat=workflow-gap
TITLE: Profile photo (avatarSrc) is only stored in localStorage — lost on any other browser/device
PAGE: profile  | COMPONENT: EK_SMS/src/components/superadmin/SAProfile.js  | BACKEND: n/a (no avatar upload endpoint)
EVIDENCE: SAProfile.js:194-205 handleSave writes the base64 avatar only to `localStorage.setItem(profileKey, ...)` (profileKey = `ek-sms-profile-${user.id}`, line 105) and to the in-memory parent via onAvatarChange; the server PATCH at line 205 deliberately sends only avatarColor, NOT avatarSrc. There is no /sa/branding/-style endpoint for the user's own avatar. The 'Upload Photo' button (line 324) and camera button therefore produce a photo that exists only on the current machine. (Note: the per-user key correctly prevents cross-account leak, and the old shared key is cleared in SuperadminDashboard.js:615 — that part is fine.)
IMPACT: Operator uploads a profile photo, sees it persist, then logs in from another device or after clearing storage and the photo is gone with no warning. Zero server persistence.
FIX: Add a real avatar upload endpoint (reuse the multipart branding pattern in routes/superadmin.js:31-40/126) storing the file under /uploads and saving the URL on the user; or clearly label the avatar as device-local.

### [46] MEDIUM · unverified · area=settings-profile · cat=ui-not-functional
TITLE: 'Manage 2FA' enrollment is an entirely non-functional preview (QR, OTP, recovery codes all illustrative)
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: n/a (no 2FA endpoint)
EVIDENCE: TwoFAView (SASettings.js:316-461): the QR is a hardcoded decorative SVG (lines 372-384), the OTP boxes are never validated, totpKey/recoveryCodes default to empty (lines 17-18) so the 'Manual Entry Key' renders blank and the recovery grid is empty, and onComplete (line 692) just toasts '2FA enrollment is a preview — nothing was changed'. The Save/Copy recovery-code buttons (lines 428-433) have no onClick. To its credit there is a clear honesty banner (lines 360-366).
IMPACT: The headline 'Manage 2FA' account-security action does nothing. Acceptable only because it is honestly labelled, but it is a dead-end screen reachable from a primary settings button.
FIX: Either implement TOTP enrollment/verification end to end, or replace the elaborate fake wizard with a single 'Two-factor authentication — coming soon' state to avoid a multi-step dead-end.

### [47] MEDIUM · unverified · area=shell-nav · cat=ui-bug
TITLE: Global search surfaces 'Enter a School' as a normal page and navigates to a StubPage instead of opening the impersonation modal
PAGE: global-search / __enter_school  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: n/a
EVIDENCE: GlobalSearch receives pages=navItems with only review/app-history/version-compare/rejection-audit/grade-audit filtered out (SuperadminDashboard.js:1573); the action item {key:'__enter_school', label:'Enter a School', action:'enter-school'} (line 912) is NOT filtered, so typing 'enter' lists it. onSelect runs goTo(item.key) (line 1575) -> setActivePage('__enter_school'). '__enter_school' is not in HANDLED_PAGES (366-389) and has no render block, so it falls through to <StubPage title={getTitle('__enter_school')} /> (line 1508-1510). getTitle has no mapping (271-360) so the title renders as '__enter School'. The sidebar handles this key specially (line 1034: item.action==='enter-school' ? setEnterSchoolOpen(true) : goTo) but GlobalSearch.onSelect does not, so from search it lands on an 'under development' stub.
IMPACT: Searching for the impersonation launcher dead-ends on a broken '__enter School' under-development placeholder instead of opening the Enter-a-School modal. Confusing dead result in the operator's primary search affordance.
FIX: Either exclude items with an `action` from the GlobalSearch `pages` list, or make GlobalSearch onSelect honor item.action the same way the sidebar does (open the enter-school modal for 'enter-school' instead of goTo).

### [48] MEDIUM · unverified · area=system-security · cat=data-mismatch
TITLE: Security log 'Status' column is always blank — backend never returns it and model has no such field
PAGE: security-logs  | COMPONENT: EK_SMS/src/components/superadmin/SASecurityLogs.js  | BACKEND: GET /api/security-logs/ -> superadminDataController.getSecurityLogs (line 133)
EVIDENCE: SASecurityLogs.js renders a 'Status' column (header line 237; cell lines 268-270) and computes `statusColor` from ev.status with cases 'Blocked'/'Flagged'/'Allowed'/'Throttled' (lines 244-248). But getSecurityLogs (superadminDataController.js:140-157) returns only {id,type,severity,actor,ip,action,ts,metadata} — no `status`. The SecurityAuditLog model (models/SecurityAuditLog.js:4-16) has no status column at all. So `ev.status` is always undefined: the Status cell renders empty and the entire statusColor branch is dead code.
IMPACT: Every row in the audit/security log shows an empty Status column, making the table look broken or incomplete. The colour-coded status legend the UI implies never appears.
FIX: Either add a `status`/outcome field to SecurityAuditLog and populate it at write time (auditLog.js), then surface it; or remove the Status column and its dead statusColor logic from SASecurityLogs.js.

### [49] MEDIUM · unverified · area=system-security · cat=ui-not-functional
TITLE: Broadcast detail 'Acknowledge & Dismiss' claims it is logged for audit but only flips local state
PAGE: alert-broadcast  | COMPONENT: EK_SMS/src/components/superadmin/SAAlertBroadcast.js  | BACKEND: n/a (no endpoint called)
EVIDENCE: In AlertDetail the acknowledgment box states 'Your acknowledgment is logged for system audit and compliance tracking' (SAAlertBroadcast.js:88-90), but the button's onClick is just `() => setAcknowledged(true)` (line 98) — no API call. The copy is also hardcoded 'This is a critical security update' (line 89) for every alert regardless of its actual severity/type. The two secondary buttons ('View Full Audit Log', 'Review School Settings', lines 107-116) are permanently disabled/no-op.
IMPACT: The UI asserts a compliance/audit guarantee it does not honour — acknowledgments vanish on re-render and are never recorded. Misleading for a security console.
FIX: Either wire acknowledge to a real persisted endpoint and remove the 'logged for audit' claim until it is, and make the severity/type copy dynamic from `alert.severity`/`alert.type` instead of hardcoded 'critical security update'.

### [50] LOW · confirmed · area=geography · cat=vague-ui
TITLE: Geography table shows raw 'Inactive' status with no indication that inactive = hidden from registration
PAGE: countries / regions / cities  | COMPONENT: EK_SMS/src/components/superadmin/SARefDataManager.js (status cell L1318-1322, toggle L1384-1394)  | BACKEND: n/a
EVIDENCE: In non-hero mode the status cell renders just `item.is_active ? 'Active' : 'Inactive'` (SARefDataManager.js:1321) and the toggle title is the bare 'Activate'/'Deactivate' (:1387). Nothing on the page explains that an inactive country/region/city is excluded from the registration dropdowns (the actual behaviour per Register.js:1391/1415/1440). Combined with the default-inactive bug above, an operator sees a freshly added, correctly-named row marked 'Inactive' and has no signal that it is therefore invisible to registrants.
IMPACT: Operators cannot tell why their newly added geography doesn't show up downstream; the active/inactive toggle reads as a cosmetic flag rather than a publish switch.
FIX: Relabel the toggle/status to convey publishing (e.g. 'Live'/'Hidden' or 'Published'/'Unpublished'), and add a subtitle/tooltip: 'Only active entries appear in the school registration dropdowns.'
VERIFY-NOTE: Confirmed but partially overstated. The cited code is accurate: in non-hero mode the status cell renders the bare label `item.is_active ? 'Active' : 'Inactive'` (SARefDataManager.js:1321) and the toggle button title is the bare 'Activate'/'Deactivate' (line 1387). There is no per-field hint, legend, or tooltip on the status column/toggle explaining that inactive geography is excluded from registration dropdowns. The downstream behaviour is real: Register.js filters geography by is_active before populating the dropdowns (countries 1391, regions 1415, cities 1440), so an inactive country/region/city is genuinely invisible to registrants. The toggle therefore is a de-facto publish switch with no UI signal saying so.\n\nWhat the finding misses: the COUNTRIES page subtitle already states 'Countries where schools can be located. Used to populate geography dropdowns during registration.' (SuperadminDashboard.js:1325), so the Countries operator does get a partial signal that this data feeds registration (though still not that the toggle is the publish switch). The REGIONS subtitle (1338) and CITIES subtitle (1352) say nothing about registration at all, so the gap is real and strongest for those two pages. This is a UX-clarity shortfall, not a functional or security defect — no data loss, the toggle works, and one of three pages carries a partial hint. The evidence also leans on a separate 'default-inactive bug above' which I could not and did not verify here; the vague-UI claim stands on its own without it.

### [51] LOW · confirmed · area=geography · cat=backend-bug
TITLE: getRegions/getCities resolve parent names with per-row findByPk (N+1 queries)
PAGE: regions / cities  | COMPONENT: n/a  | BACKEND: GET /api/regions, GET /api/cities -> getRegions/getCities
EVIDENCE: getRegions (superadminDataController.js:2002-2009) maps each region through `await Country.findByPk(r.country_id)` inside Promise.all — one extra SELECT per region. getCities (:2087-2098) is worse: per city it issues `Country.findByPk` AND `Region.findByPk` (two extra SELECTs per row). With no association/include and no caching, listing M cities fires ~2M+1 queries.
IMPACT: Slow list loads as geography grows; unnecessary DB load on every dropdown fetch (these GETs are also public/unauthenticated, so externally hittable).
FIX: Define Sequelize associations (Region.belongsTo(Country), City.belongsTo(Country)/belongsTo(Region)) and use a single findAll with include, or fetch all parent names in one bulk query keyed by id and map in memory.

### [52] LOW · confirmed · area=masterdata-catalog · cat=backend-bug
TITLE: New catalog rows are created is_active=false by default (model default), so they are inactive on creation
PAGE: institution-type / school-type / school-capacity / syllabus-type / class-subtype  | COMPONENT: n/a  | BACKEND: POST /api/institution-types/ → createInstitutionType (and createSchoolType/createCapacityCategory/createSchoolCapacity/createSyllabusType/createClassSubtype)
EVIDENCE: Models default is_active to false: CapacityCategory.js:7, InstitutionType.js:7, SchoolType.js:7, SyllabusType.js:7, ClassSubtype.js:7, SchoolCapacity.js:8 all `is_active: { ... defaultValue: false }`. The create handlers never set is_active — e.g. superadminDataController.js:1557 `InstitutionType.create({ name: ... })`, :1781 CapacityCategory, :1867 SchoolCapacity, :2195 SchoolType, :2263 SyllabusType, :2327 ClassSubtype. Consumers filter on is_active (Register.js:1391/1415/1440 use `.filter(c => c.is_active)`), so a freshly-added type is created inactive and is excluded from any is_active-filtered dropdown until the SA notices and manually toggles it on. (AcademicSystem.js:7, GradingSystem.js:7, LessonPlanType.js:8 correctly default to true — the inconsistency itself is a smell.)
IMPACT: After a super admin clicks Add and sees a success toast, the new institution/school/capacity/syllabus/class-subtype is silently inactive and won't surface to schools until they also hit the toggle. There is no in-form hint that Add ≠ Active for these taxonomies, so rows look added-but-broken.
FIX: Either set `is_active: true` in the create handlers for these taxonomies (matching AcademicSystem/GradingSystem), or change the model defaultValue to true. If inactive-on-create is intentional, surface it in SARefDataManager (the create note + an explicit 'Activate' prompt), and make it consistent across all nine types.
VERIFY-NOTE: The defect is real but its consequences are overstated. New catalog rows are indeed created is_active=false (models default false + create handlers omit the field), inconsistent with the three sibling catalogs that default true, and the SARefDataManager create form for these non-year pages shows no "Add != Active" hint. But the claimed downstream breakage is wrong: the cited Register.js filter lines (1391/1415/1440) apply to countries/regions/cities, not these taxonomies; Register.js uses hardcoded DEFAULT_INSTITUTION_TYPES and discards the /api/institution-types/ response; all GET endpoints return rows unfiltered; and no consumer anywhere filters these six catalogs by is_active. The practical effect is a cosmetic/consistency UX issue (the SA must toggle the new row on), not a functional break that hides types from schools. Severity is low, not high.

### [53] LOW · confirmed · area=masterdata-catalog · cat=data-mismatch
TITLE: Public (unauthenticated) catalog GETs return inactive rows; clients must filter client-side
PAGE: institution-type / academic-system / grading-system  | COMPONENT: n/a  | BACKEND: GET /api/institution-types|academic-systems|grading-systems (public, routes/superadmin.js:88,92,93)
EVIDENCE: routes/superadmin.js:87-93 mounts these three GETs publicly before `router.use(authenticateToken)` (:95). The handlers return every row regardless of is_active — getInstitutionTypes (:1544) `InstitutionType.findAll({ order: [...] })` with no where clause; same for getAcademicSystems (:2539) and getGradingSystems (:2584). Every consumer is expected to filter is_active itself (Register.js does this for countries/regions/cities at :1391/1415/1440 but, per the dead-wiring finding above, never even reads these three).
IMPACT: Inactive/retired catalog entries are exposed on a public endpoint and would appear in any consumer that forgets to filter is_active. Combined with the is_active=false-on-create default, the contract is fragile.
FIX: Have the public GET variants return only is_active rows (add `where: { is_active: true }`), or expose a documented `?active=1` param and use it from public consumers; keep the SA-authenticated list returning all rows for management.
VERIFY-NOTE: Two parts of the finding are overstated/mis-stated. (1) The "is_active=false-on-create default" amplifier is true ONLY for InstitutionType (model defaultValue:false). AcademicSystem and GradingSystem default is_active:true (models confirmed), so newly created rows there are active — the "fragile contract" argument applies to 1 of 3, not all 3. (2) The finding implies countries/regions/cities filter server-side; they do not — getCountries/getRegions/getCities also do findAll with no where clause and filtering is client-side, so the contract is uniform across all public catalog GETs, not inconsistent. Also note the SAME handlers serve the authenticated SA reference-data management pages (sa.get at 151/201/206) which legitimately must see inactive rows to toggle them — so the handler returning all rows is correct for that surface; only the unauthenticated public mount is debatable, and even then it exposes only low-sensitivity, non-PII master-data names with no live consumer surfacing them. This is best characterized as informational/hardening (drop the unused public institution-types/academic-systems/grading-systems mounts or add a server-side is_active filter to a separate public-only handler), not a functional data-mismatch bug.

### [54] LOW · confirmed · area=people-access · cat=ui-not-functional
TITLE: 'Clone role' button on every governance role card has no handler (dead onClick)
PAGE: governance  | COMPONENT: EK_SMS/src/components/superadmin/SAGovernance.js  | BACKEND: n/a
EVIDENCE: Each role card renders `<button className="sa-role-icon-btn" aria-label="Clone role"><IcCopy size={16} /></button>` with no onClick (SAGovernance.js:515). Clicking it does nothing.
IMPACT: A visible, affordance-bearing action button that silently no-ops — operators expect to clone a role to bootstrap a new one and get no response or feedback.
FIX: Wire the clone button to open CreateRoleModal pre-filled with the role's name+description (and copy its perms), or remove the button until clone is implemented.

### [55] LOW · confirmed · area=people-access · cat=ui-quality
TITLE: User Profile 'Active Sessions' / Terminate / risk stats render but are local-only fabrications with no backend
PAGE: users  | COMPONENT: EK_SMS/src/components/superadmin/SAUsers.js  | BACKEND: GET /api/users/ returns sessions:[] and zeroed risk fields (mapUserToSaRow)
EVIDENCE: getUsers/mapUserToSaRow hardcodes `sessions: []`, `recentActivity: []`, `riskScore:0`, `failedAttempts:0`, `successLogins:0`, `twoFAEnabled:false` for every user (superadminDataController.js:302-310). In the profile, terminateSession/terminateAll just filter local React state (SAUsers.js:208-209) with no API call, so 'Terminate' / 'Terminate All' do nothing server-side. Since sessions is always [], the Active Sessions card always shows the empty state. Risk profile stats are therefore always 0 and the 'Risk Assessment Score' bar is always 0/100.
IMPACT: The profile presents a security console (sessions to terminate, risk score, login counts) that is entirely inert — no user ever has data, and the terminate buttons can't terminate anything. The code comments already acknowledge most of this; the remaining issue is that the empty/zero state isn't framed as 'not yet tracked' for sessions the way the risk bar is.
FIX: Either back sessions/login-counters with real data (a sessions table + counters) and make terminate hit an endpoint, or label the Active Sessions and risk stats as 'not yet tracked' (as the risk bar already is) so operators don't mistake an empty list for 'no active sessions'.
VERIFY-NOTE: The factual core is confirmed but the title/impact overstate what actually renders. Backend mapUserToSaRow (superadminDataController.js:288-312) hardcodes sessions:[], recentActivity:[], riskScore:0, failedAttempts:0, successLogins:0, twoFAEnabled:false, alertsTriggered:0 for EVERY user, and getUsers (314-332) maps all rows through it — so confirmed. terminateSession/terminateAll (SAUsers.js:208-209) only filter local React state with no ApiClient call — confirmed inert. HOWEVER: because sessions is always [], the Active Sessions card always hits the empty branch (line 424-425 'No sessions found'); `sessions.some(s=>s.active)` (line 415) is always false so the 'Terminate All' button NEVER renders, and the per-session 'Terminate' button (line 452, guarded by sess.active) never mounts either. So the claim that 'Terminate buttons render but do nothing' is wrong — they never render at all for any real user; the inert handlers are dead code that can't be reached through the UI. A prior cleanup pass already mitigated most of this: trend-delta chips removed (307-308), the fabricated risk sparkline deleted (33-35, 328-330), the risk bar relabeled 'not yet tracked' (333), and 2FA shows 'isn't available yet' (356). The ONLY genuine residual defect is the one the finding itself identifies as remaining: the sessions empty-state copy says 'No sessions found' (line 425) rather than being framed 'not yet tracked' like the risk bar, which misleads the operator into thinking sessions are tracked but this user simply has none. That is a low-severity honesty/copy nuance, not a functional or security bug.

### [56] LOW · confirmed · area=reports-analytics · cat=data-mismatch
TITLE: Reports Hub count chips never populate — /api/superadmin/dashboard/ is a 404, swallowed silently
PAGE: reports  | COMPONENT: EK_SMS/src/components/superadmin/SAReportsHub.js  | BACKEND: real route is GET /api/dashboard/ (routes/superadmin.js:99)
EVIDENCE: SAReportsHub.js:57 calls `apiGet('/api/superadmin/dashboard/')` inside useEffect, but the route is `router.get('/dashboard/', getDashboard)` mounted at '/api' (superadmin.js:99) => real path `/api/dashboard/`. The wrong path 404s; the `.catch(() => {})` at line 59 swallows it. getDashboard (superadminController.js:498-509) does return `schools` and `total_users` as counts, so the shape is fine — only the path is wrong.
IMPACT: The Schools Register card 'N schools' and Platform Users card 'N users' count badges never appear (they stay blank). Cosmetic vs the export bug, but it makes the hub look unpopulated even when data exists.
FIX: Change SAReportsHub.js:57 from '/api/superadmin/dashboard/' to '/api/dashboard/'.
VERIFY-NOTE: Defect is real and accurately stated; only the severity is overstated. It is purely cosmetic — two count badges ('N schools', 'N users') never render because the dashboard fetch 404s on the wrong '/api/superadmin/dashboard/' path (real path '/api/dashboard/'). The grades chip and all exports work. Fix: change line 57 to apiGet('/api/dashboard/'). Note the same wrong path also exists in App.js:144 and the controller's own comment (superadminController.js:497) mislabels the route, which is the root cause of the confusion.

### [57] LOW · confirmed · area=reports-analytics · cat=data-mismatch
TITLE: Top-Schools 'Perf. Index' is a relative student-count ratio mislabeled as a performance/integrity metric
PAGE: benchmarks  | COMPONENT: EK_SMS/src/components/superadmin/SABenchmarks.js  | BACKEND: n/a (derived client-side)
EVIDENCE: SABenchmarks.js:234 computes `perf: parseFloat((s.student_count / Math.max(1, schoolStats[0]?.student_count || 1) * 10).toFixed(1))`. The card header (315) calls the table 'Ranked by Integrity & Performance Index' and the column (325) is 'Perf. Index', but the value is purely a normalized headcount ratio — nothing to do with grades, integrity, or academic performance. With getSchoolStats returning 0 students for all schools, every perf value is also 0/NaN.
IMPACT: The Super Admin reads 'Performance Index' / 'Integrity' as an academic-quality ranking; it is actually 'this school has X% as many students as the biggest school'. Combined with the zero-count bug, the column is both mislabeled and empty.
FIX: Either rank by a real academic metric (avg_total / pass rate from grade data, which getGradeStats.per_school could surface), or relabel the column 'Relative Size' and drop the 'Integrity & Performance' header wording.
VERIFY-NOTE: The perf value is uniformly 0, not "0/NaN" — the `|| 1` and `Math.max(1, ...)` guards prevent any NaN. More significantly, the "Perf. Index" column is not actually reached in practice: getSchoolStats returns rows without an `is_approved` field, so the frontend's `.filter(s => s.is_approved)` (SABenchmarks.js:197) produces an empty array, topSchools is empty, and the table renders the "No approved schools on the platform yet." empty state (:320) rather than any perf values. The accurate statement: line 234's `perf` is a relative student-headcount ratio (each school's student_count / the largest school's student_count * 10) mislabeled as a "Perf. Index" / "Integrity & Performance Index" (lines 315, 325) — a real semantic data-mismatch — but it is a label/derivation defect on a read-only page whose rows currently don't render at all, so severity is low rather than medium.

### [58] LOW · confirmed · area=reports-analytics · cat=ui-not-functional
TITLE: 'View All Schools' button and SABenchmarks onNavigate prop are dead; SchoolCard options menu is a no-op
PAGE: benchmarks / analytics  | COMPONENT: EK_SMS/src/components/superadmin/SABenchmarks.js; EK_SMS/src/components/superadmin/SAAnalytics.js  | BACKEND: n/a
EVIDENCE: SABenchmarks.js:343 `<button className="san-view-all-btn">View All Schools</button>` has no onClick — clicking does nothing. SuperadminDashboard.js:1244 passes `<SABenchmarks onNavigate={goTo} />` but the component signature is `export default function SABenchmarks()` (181) and never references onNavigate, so the wired nav handler is discarded (the 'View All Schools' button should have used it). In SAAnalytics, the per-card 'options' kebab (SAAnalytics.js:301) only `e.stopPropagation()` with no menu.
IMPACT: Dead-end controls: the operator clicks 'View All Schools' and nothing happens, and the card kebab menu does nothing. Low severity but they read as broken UI.
FIX: Wire 'View All Schools' to `onNavigate('analytics')` (accept the prop in SABenchmarks), and either implement or remove the SchoolCard kebab button.

### [59] LOW · confirmed · area=schools-onboarding · cat=dead-nav
TITLE: SAVersionCompare page is dead code — no UI path navigates to it
PAGE: version-compare  | COMPONENT: EK_SMS/src/components/superadmin/SAAppHistory.js, EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: n/a
EVIDENCE: SuperadminDashboard.js:1153-1158 renders SAAppHistory with an onCompare prop: `<SAAppHistory school={...} onBack={...} onCompare={() => handleCompare(selectedSchool)} />`. But SAAppHistory.js:24 declares `export default function SAAppHistory({ school, onBack })` — it never destructures or calls onCompare, and its JSX (lines 48-131) has no Compare button. handleCompare (SuperadminDashboard.js:564-567) sets activePage='version-compare', and the page is registered (line 367, rendered at 1161-1168), but nothing else triggers it. So SAVersionCompare is completely unreachable from the running app.
IMPACT: An entire built page (SAVersionCompare) can never be opened by the operator. The wiring looks complete but the trigger button was removed from SAAppHistory, leaving orphaned code and a confusing half-wired feature.
FIX: Either drop SAVersionCompare + handleCompare + the version-compare route entirely (it is honestly just a single-submission read-only view that duplicates SAReview), or add a real 'Review submission' button inside SAAppHistory that destructures and calls onCompare.
VERIFY-NOTE: Finding is accurate as written. Only adjustment is severity: this is orphaned/dead-nav code with no functional, data, or security impact. SAReview already provides the full approve/reject/request-changes workflow, and SAVersionCompare itself is just a read-only single-submission view (it openly states EK-SMS stores no prior revisions to diff). The operator loses nothing reachable; this is a code-cleanliness defect, so severity is low rather than medium. Fix is either to delete SAVersionCompare + handleCompare + the version-compare route/array entries, or to add a Compare/Review button in SAAppHistory that destructures and calls onCompare.

### [60] LOW · confirmed · area=schools-onboarding · cat=ui-quality
TITLE: 'Avg Review' stat shows a hardcoded fake trend '−10m faster'
PAGE: applications  | COMPONENT: EK_SMS/src/components/superadmin/SAApplications.js  | BACKEND: n/a
EVIDENCE: SAApplications.js:94 `{ label: 'Avg Review', value: avgReview, ... trend: { dir: 'down', label: '−10m faster' } }`. The trend direction and label are constants — they never reflect any real measurement and contradict the (already meaningless) avgReview value.
IMPACT: Presents an invented performance figure as a real metric. Misleads the operator about review-time improvement.
FIX: Remove the hardcoded trend or compute it from real period-over-period data once a true approved_at timestamp exists.
VERIFY-NOTE: The trend object (dir + label '-10m faster') is the fake part and is real. However, the evidence overstates one point: the avgReview VALUE itself is NOT meaningless — lines 79-88 genuinely compute the mean elapsed time between registration_date and approval_date across approved schools (falling back to '—' when no dated approvals exist). So the value is a real measurement; only the '-10m faster' trend beneath it is invented and unsupported by any historical baseline.

### [61] LOW · confirmed · area=schools-onboarding · cat=ui-bug
TITLE: Administrator section renders two fields both labeled 'Email'
PAGE: review  | COMPONENT: EK_SMS/src/components/superadmin/SAReview.js  | BACKEND: n/a
EVIDENCE: SAReview.js:358 `<Field label="Email" value={adminEmail} />` and again SAReview.js:361-365 `<Field label="Email" value={adminEmail && adminEmail !== '—' ? 'Provided' : 'Not provided'} chip={...} />`. The second 'Email' row is actually an email-presence status chip but reuses the same label, so the operator sees 'Email: admin@x.com' followed by 'Email: Provided'.
IMPACT: Confusing duplicated label; the second row reads as a contradictory second email value rather than a verification status.
FIX: Relabel the second field to 'Email status' (or 'Contact verified') so the two rows are distinguishable.

### [62] LOW · confirmed · area=schools-onboarding · cat=ui-quality
TITLE: Field-completeness heuristic is dressed up as a precise 'Risk' / trust score
PAGE: review  | COMPONENT: EK_SMS/src/components/superadmin/SAReview.js, EK_SMS/src/components/superadmin/SAApplications.js  | BACKEND: n/a
EVIDENCE: SAReview.js:52-60 getRisk returns fixed trust values 94/62/28 purely from how many of 3 field groups are filled (city, phone+email, principal). The header labels this '/100' 'Profile Completeness' (SAReview.js:261-262) while the mobile card (SAReview.js:394-404) labels the SAME number 'Risk Assessment'. SAApplications.js:43-51,213-215 stamps each pending school as 'High/Medium/Low Risk'. There is no fraud/risk signal — it is a 3-point checkbox count rendered as a 28/62/94 'trust score'.
IMPACT: Operator may treat a school as fraud-'high risk' when the only issue is a missing phone number. The two inconsistent labels (Risk vs Profile Completeness) for the same value compound the confusion.
FIX: Pick one honest label everywhere ('Profile completeness'), and either show the actual count (e.g. '2 of 3 sections complete') or list which fields are missing instead of a fake precise score.
VERIFY-NOTE: The Risk vs Profile Completeness inconsistency is not limited to the mobile card vs desktop header. On the SAReview desktop view the SAME getRisk output simultaneously powers a "Profile Completeness {trust}/100" gauge (line 261-262) and a "Low/Medium/High Risk" status chip in the identity header (riskLabel, lines 180 and 244-247). So the "Risk" framing of a field-completeness count appears on desktop too, making the dual-labeling slightly more pervasive than the finding stated. Substance of the finding is otherwise correct.

### [63] LOW · confirmed · area=schools-onboarding · cat=workflow-gap
TITLE: All-Schools rows have no 'Enter as admin' action despite the design intent
PAGE: schools  | COMPONENT: EK_SMS/src/components/superadmin/SASchools.js  | BACKEND: POST /api/impersonate/ (impersonate) — works, just not invoked here
EVIDENCE: SuperadminDashboard.js:908 comments the Schools section as 'the tenant directory + onboarding pipeline (impersonate from here)', and SASchools receives an onAction prop (line 1232). But SASchools.js's row menu (lines 221-232) renders only a single 'View Details' item that calls onReview; onAction is never used and there is no Enter/Impersonate button. Impersonation is only reachable via the separate '__enter_school' nav item modal (SuperadminDashboard.js:1581+).
IMPACT: The documented 'impersonate from the Schools list' flow does not exist on the list rows; the unused onAction prop is dead wiring. Operators must use a different nav entry to enter a school they are already looking at.
FIX: Add an 'Enter as admin' menu item to the SASchools row dropdown (shown only for approved+active schools) that calls the impersonation handler, or remove the unused onAction prop and the misleading comment.

### [64] LOW · confirmed · area=schools-onboarding · cat=ui-quality
TITLE: All-Schools / Onboarding show 'No schools found' during initial load (no loading/skeleton)
PAGE: schools  | COMPONENT: EK_SMS/src/components/superadmin/SASchools.js, EK_SMS/src/components/superadmin/SAApplications.js  | BACKEND: GET /api/schools/
EVIDENCE: SASchools/SAApplications receive only the `schools` array; neither gets an isLoading flag. fetchSchools (SuperadminDashboard.js:475-481) starts with schools=[] (line 444) and swallows errors silently (`catch { /* network error — silently ignore */ }`). So before the fetch resolves, or if it fails, SASchools.js:137-144 and SAApplications.js:184-193 render the empty state ('No schools found' / 'No pending applications') indistinguishably from a real empty result, and a failed fetch surfaces nothing at all.
IMPACT: On a slow network the operator briefly sees a false 'No schools' message; on a fetch failure they permanently see 'No schools' with no error surface, hiding outages.
FIX: Pass an isLoading flag and render a skeleton/spinner, and surface fetch failures (e.g. a retry banner) instead of silently swallowing them into an empty state.
VERIFY-NOTE: The loading-state portion of this finding is a false positive: a fullscreen loader (SuperadminDashboard.js:637-643) is shown for the entire initial fetch, so the operator never sees a transient false "No schools found" during loading. The genuine, narrower defect is silent error handling: fetchSchools (line 475-481) catches and ignores network errors while still clearing isLoading, so a FAILED /api/schools/ fetch renders the dashboard with schools=[] and surfaces only the generic empty state ("No schools found" / "No pending applications") with no error toast or retry — hiding a backend outage from the operator. Fix is to track a fetch-error flag and render a distinct error state, not to add a skeleton.

### [65] LOW · confirmed · area=shell-nav · cat=responsiveness
TITLE: SuperadminDashboard.css is empty (no rules, no @media) — all shell responsiveness depends entirely on SA.css
PAGE: shell (global)  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.css  | BACKEND: n/a
EVIDENCE: SuperadminDashboard.css contains only two comment lines and zero CSS rules / zero @media blocks (full file is 2 lines). The shell's responsive behavior (off-canvas sidebar at max-width:768px, mobile bottom nav, search-modal width clamps) is actually implemented in SA.css (e.g. SA.css:1807-1821 sidebar transform + .sa-mobile-nav display:flex, :3606 .sa-search-modal max-width clamp, .sasc-bar @media at SASchoolScope.css:139). So the shell IS responsive, but the file named for the dashboard carries none of it — a maintainability/colocated-CSS gap, not a runtime defect.
IMPACT: Low: shell renders responsively because SA.css covers it. Risk is future edits to SuperadminDashboard.css assume it controls the shell when it does not; the empty file is misleading.
FIX: Either remove SuperadminDashboard.css (it is import-only with no rules) or move the shell-layout @media rules from SA.css into it so the file name matches its responsibility.

### [66] LOW · confirmed · area=shell-nav · cat=ui-quality
TITLE: GlobalSearch modal is built with inline styles and relies on a single CSS class for its mobile width clamp
PAGE: global-search  | COMPONENT: EK_SMS/src/components/superadmin/SuperadminDashboard.js  | BACKEND: n/a
EVIDENCE: The GlobalSearch modal panel is styled almost entirely inline (SuperadminDashboard.js:196-200) with only className="sa-search-modal" providing width; the width/clamp comes from SA.css (.sa-search-modal max-width:540px default, clamped to calc(100vw - 32px) at max-width:600px, SA.css:3582/3606). Result rows are buttons with ~9px vertical padding (line 239) giving touch targets under the 44px project minimum on mobile. Functionally the modal does navigate (onSelect -> goTo, line 1575) and closes correctly, so this is quality only.
IMPACT: Low: search rows are slightly short for comfortable touch on phones, and the heavy inline styling makes the modal harder to theme/maintain consistently with the rest of the console.
FIX: Move the modal styling into SA.css classes and bump result-row min-height to >=44px on touch to meet the project touch-target rule.
VERIFY-NOTE: The touch-target shortfall is not uniform across all rows, so the row-height claim is slightly overstated. Page-result rows (20px icon + 9px*2 padding ≈ 38px) fall under 44px, but school-result rows use a 28px avatar (28 + 18 ≈ 46px) and meet the minimum. So the defect applies specifically to page-result rows, not every result row. The structural claims (all-inline styling, single class for width clamp, no min-height on the buttons) are fully accurate.

### [67] LOW · confirmed · area=years-terms · cat=backend-bug
TITLE: closeAcademicYear can close any year (incl. a draft) and leaves the platform with no active year, with no confirmation
PAGE: academic-year  | COMPONENT: EK_SMS/src/components/superadmin/SARefDataManager.js  | BACKEND: POST /api/academic-years/:id/close/ -> closeAcademicYear (superadminDataController.js:1288)
EVIDENCE: closeAcademicYear (:1288-1312) unconditionally sets `is_active=false`, `status='closed'` with no check that the row is currently active or that another year will take over. The UI close-out card (SARefDataManager.js:899-921) is only shown for the active year near/after its end date, but the endpoint itself has no guard, and after closing, renderHero falls back to 'No active year' (:830-837) with the platform having zero active academic year until a new rollout. There is no second-step confirm on the close-out button (:911-918) — a single click closes the live year for every school.
IMPACT: One click (or a direct API call) can drop the platform to no-active-year state, breaking any feature that resolves 'current year' for all tenants, with no undo prompt.
FIX: Guard closeAcademicYear to only act on the active year, require a confirm step (like the rollout modal), and either block closing until a successor is rolled out or warn explicitly that schools will be left with no active year.
VERIFY-NOTE: The defect is real but its impact is overstated. Accurate version: closeAcademicYear lacks a guard that the target row is currently active (so a draft/closed year can be re-marked 'closed' via direct API), and the UI close button has no confirmation dialog. But it is NOT a no-undo platform-breaking action: closing is reversible (toggleAcademicYearStatus reactivates a closed year; rolloutAcademicYear re-activates one), a zero-active state is the intended transient between close and next rollout, per-school AcademicYear records are unaffected so tenant 'current year' resolution does not break platform-wide, and the only UI-exposed close target is the active year past 90%/end-date. The real, low-severity fixes are: add an is_active/status guard (or no-op if already closed) in closeAcademicYear, and add a confirm step before handleClose.

### [68] LOW · unverified · area=grade-integrity · cat=responsiveness
TITLE: SAGradeIntegrity and SAGradeAuditDetail rely on shared sa-gi-* classes with no verified mobile rules
PAGE: grade-integrity  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeIntegrity.js, SAGradeAuditDetail.js:105  | BACKEND: n/a
EVIDENCE: Neither SAGradeIntegrity.js nor SAGradeAuditDetail.js imports a CSS file; both use sa-gi-* classes (sa-gi-card, sa-gi-compare, sa-gi-state-compare, sa-gi-actor, sa-gi-evidence, sa-gi-ledger) that are not defined in SAGradesAccumulation.css (the only bucket css). SAGradeAuditDetail wraps everything in `maxWidth:760` (line 105) and uses a fixed 2-col evidence grid (sa-gi-evidence). Whether these classes carry @media max-width:600px rules cannot be confirmed from the audited files - they must live in a global SA.css. If those rules are absent, the grade comparison boxes and evidence grid will not stack on mobile.
IMPACT: Potential mobile layout breakage on the integrity dashboard cards and audit detail comparison/evidence grids if the shared sa-gi-* classes lack mobile breakpoints.
FIX: Verify SA.css contains @media (max-width:600px) rules for .sa-gi-card, .sa-gi-compare, .sa-gi-state-compare, .sa-gi-evidence, .sa-gi-actor-meta, .sa-gi-ledger; add stacking rules if missing.

### [69] LOW · unverified · area=grade-integrity · cat=ui-quality
TITLE: Grade Reports chart hardcodes mid-term/exam-period x-axis labels and a fixed 'anomaly spike' at day 20
PAGE: grade-report  | COMPONENT: EK_SMS/src/components/superadmin/SAGradeReport.js:398-429  | BACKEND: n/a
EVIDENCE: The modification-frequency chart draws a fixed anomaly marker line and red peak dot at array index 20 unconditionally (lines 399-420: `x1={(20/(chartData.length-1))*400}` and `cy={140 - (chartData[20]/Math.max(...chartData))*130}`), regardless of where real activity is. The x-axis labels are hardcoded 'Week 1 / Week 2 / Mid-Term / Exam Period / Finals' (lines 425-429) even though chartData is a per-day count over 30/90 days, not term phases. The legend advertises an 'Anomaly Spike' that is purely decorative. With sparse real data, chartData[20] may be 0, putting the red 'peak' dot at the top of an empty chart.
IMPACT: The chart implies a detected exam-period anomaly that is not derived from data - misleading on the integrity USP, and the labels do not match the actual 30/90-day daily axis.
FIX: Drop the hardcoded day-20 spike marker and the term-phase x-axis labels; render axis ticks from the real date range and only mark a peak at the actual max-value index (or remove the anomaly decoration until real anomaly detection exists).

### [70] LOW · unverified · area=notifications · cat=ui-quality
TITLE: No loading state while security-logs / admin-settings fetches are in flight
PAGE: notifications  | COMPONENT: EK_SMS/src/components/superadmin/SANotifications.js  | BACKEND: GET /api/security-logs/, GET /api/admin-settings/
EVIDENCE: The useEffect fires two ApiClient.get calls (SANotifications.js:214,217) with no loading flag. notifications starts as [] (line 229) and filtered.length===0 immediately renders the 'No notifications to show' empty card (line 345-349). So during the brief fetch window — or if schools/gradeAlerts props arrive late — the user sees a definitive 'No notifications' message that then flips to a populated list. Errors are swallowed with empty .catch(() => {}) (lines 216,221) so a failed security-logs/admin-settings call surfaces nothing at all.
IMPACT: Momentary 'No notifications to show' flash that contradicts reality; silent failure of the security/system feeds means an operator can't tell 'genuinely empty' from 'fetch failed'.
FIX: Add an isLoading flag covering the initial fetches and the schools/gradeAlerts props, render a spinner/skeleton instead of the empty-state until resolved, and surface a small error notice if the security-logs or admin-settings fetch rejects rather than silently swallowing it.

### [71] LOW · unverified · area=rejected · cat=data-mismatch
TITLE: Reconsider promises 'return to pending review' but backend sets changes_requested, putting the school in a different state
PAGE: rejection-audit  | COMPONENT: EK_SMS/src/components/superadmin/SARejectionAudit.js  | BACKEND: handleSchoolAction request_changes branch superadminController.js:192-206
EVIDENCE: SARejectionAudit.js:353 tells the operator 'Reconsider this application and return it to pending review for re-evaluation.' But handleReconsider sends action 'request_changes' (SuperadminDashboard.js:575), and the backend sets `school.changes_requested = true` (superadminController.js:203) in addition to clearing rejection_reason/is_active. So the school is NOT in plain 'pending' — it is in 'changes_requested', which the login gate and deriveApprovalStatus (registrationController.js:30) treat distinctly, and which signals the school must edit/resubmit rather than simply await re-review.
IMPACT: The recovery-action copy mis-describes the resulting state. An operator expecting the school to sit untouched in the pending queue instead pushes it into a 'changes requested' state that asks the school to modify its submission.
FIX: Either change the copy to say 'returns the school for changes/re-submission', or add a true 'reopen as pending' action that clears rejection without setting changes_requested, matching the stated intent.

### [72] LOW · unverified · area=rejected · cat=ui-quality
TITLE: Rejection audit shows the fabricated timeline during the initial load instead of a loading state
PAGE: rejection-audit  | COMPONENT: EK_SMS/src/components/superadmin/SARejectionAudit.js  | BACKEND: n/a
EVIDENCE: SARejectionAudit.js:140 `isReal = realEvents.length > 0`, and line 157 `timeline = isReal ? [...] : [...fallbackEvents...]`. On first paint `loading` is true and `logs` is still empty, so isReal is false and the component immediately renders the reconstructed fallback timeline and the 'Reconstructed' badge; only the small caption at line 252-254 switches to 'Loading live audit events…'. The body timeline shows fabricated events before the real fetch resolves, then snaps to the real trail.
IMPACT: For a moment every audit view shows fake events and a 'Reconstructed' badge even when real data is about to arrive, which is confusing and undermines trust in the audit page.
FIX: Render a skeleton/spinner for the timeline body while `loading` is true and only choose real-vs-fallback after the fetch settles.

### [73] LOW · unverified · area=settings-profile · cat=backend-bug
TITLE: 'Record Backup Entry' writes a timestamp + fabricated filename/size but produces no actual backup
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: POST /api/sa/backup/manual/ -> postSaBackupManual
EVIDENCE: postSaBackupManual (superadminDataController.js:673-705) fabricates `backupFilename = eksms-backup-<ts>.sql` and a hardcoded `estimatedSizeBytes = 2048000` ('~2MB estimated'), stores them in settings.last_backup_meta, and returns them as if real (lines 696-700). No DB dump is created. The UI is honest about this (lines 1155-1158 and toast 'no database dump is produced yet'), and the button label was renamed to 'Record Backup Entry'.
IMPACT: Low because it is disclosed, but the response still presents a concrete filename and size that imply a real artifact exists; an operator could mistake the entry for a recoverable backup.
FIX: Don't emit a fake .sql filename and fixed 2MB size for a backup that doesn't exist — either run mysqldump and report the true file/size, or store only the timestamp + an explicit 'no artifact' marker.

### [74] LOW · unverified · area=settings-profile · cat=ui-not-functional
TITLE: Emergency Lockdown records state + audit log only; enforcement is unimplemented (returns sessions_terminated:0)
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: POST /api/sa/lockdown/ -> postSaLockdown
EVIDENCE: postSaLockdown (superadminDataController.js:641-671) writes lockdown_state into the settings JSON and appends an audit entry, then returns `affected: { sessions_terminated: 0, grades_locked: 0 }` (line 665) — nothing blocks logins, kills sessions, or locks grades regardless of the chosen protocol (grade-lock/login-suspend/full-blackout). The UI is consistently honest about this (LockdownActive lines 477-505, confirm modal lines 1276-1278, status pill 'Recorded (not enforced)').
IMPACT: Low/disclosed: the operator can log a lockdown for audit but it has no protective effect; the three distinct 'Protocol' radio options are cosmetic since all behave identically.
FIX: Implement at least login-suspension enforcement keyed off lockdown_state in the auth middleware, or collapse the three identical-behaviour protocol options until enforcement exists.

### [75] LOW · unverified · area=settings-profile · cat=backend-bug
TITLE: Server-side change-password accepts any non-empty new password (no length/strength check)
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js, SAProfile.js  | BACKEND: POST /api/change-password/ -> postChangePassword
EVIDENCE: postChangePassword (superadminDataController.js:233-257) only checks both fields present and that current_password matches via bcrypt.compare; it then hashes whatever new_password is given with no minimum length/complexity validation and no 'must differ from current' check. The client enforces strength (calcStrength + strengthLvl>=3 in SASettings PasswordView and SAProfile), but a direct API call could set a 1-char password.
IMPACT: Client-only password policy; the documented platform security posture (12+ chars, complexity) is bypassable via the API.
FIX: Mirror the client policy server-side in postChangePassword (reject new_password shorter than 12 / failing complexity) and reject new === current.

### [76] LOW · unverified · area=settings-profile · cat=workflow-gap
TITLE: Branding logo/favicon upload persists a URL but is never applied to the running app
PAGE: settings  | COMPONENT: EK_SMS/src/components/superadmin/SASettings.js  | BACKEND: POST /api/sa/branding/ -> postSaBranding
EVIDENCE: postSaBranding (superadminDataController.js:608-628) saves the uploaded file under /uploads/branding and stores its URL in settings.branding_logo/branding_favicon — this part is real (multer wired at routes/superadmin.js:126). But nothing consumes branding_logo.url to actually render the platform logo or swap the document favicon; the SuperadminDashboard shell and index.html don't read it. The General-tab Branding boxes (SASettings.js:1076-1097) only echo the preview back.
IMPACT: Operator uploads a system logo/favicon and it appears to save, but the console/app branding never changes — a half-wired feature with no visible result anywhere.
FIX: Consume settings.branding_logo.url in the dashboard header/login and inject branding_favicon into the document <link rel=icon>, or label the upload as 'stored for future use'.

### [77] LOW · unverified · area=system-security · cat=ui-not-functional
TITLE: Forensic detail 'Acknowledge & Dismiss' and 'Flag for Review' do not persist; chain-of-custody is illustrative
PAGE: forensics  | COMPONENT: EK_SMS/src/components/superadmin/SAForensics.js  | BACKEND: n/a (no endpoint called)
EVIDENCE: In ForensicDetail, 'Acknowledge & Dismiss' onClick is `() => setAcknowledged(true)` (SAForensics.js:229) — local only, never marks the ForensicEvent resolved on the server. 'Flag for Review' (lines 223-225) has no onClick at all (dead button). The Chain of Custody (CHAIN_STEPS, lines 22-28) and the SHA-256 hash comparison are static/derived from metadata; the page itself is honest about this ('Illustrative — not a cryptographic chain' line 187; 'Cryptographic signing... not yet implemented' line 218), so this is lower severity, but the action buttons still imply functionality that does not exist.
IMPACT: Operator cannot actually resolve or flag a forensic case from the detail view; the buttons are decorative.
FIX: Wire Acknowledge to a PATCH that sets ForensicEvent.resolved/resolved_at, and either implement 'Flag for Review' or remove it. (Lower priority while the page has no data — see forensics-never-populated.)

### [78] LOW · unverified · area=system-security · cat=ui-quality
TITLE: System Health 'Recent Activity' card is a hardcoded empty placeholder
PAGE: system-health  | COMPONENT: EK_SMS/src/components/superadmin/SASystemHealth.js  | BACKEND: GET /api/system-health/ -> superadminController.getSystemHealth (line 380)
EVIDENCE: SASystemHealth.js:164-169 renders a 'Recent Activity' card whose body is a fixed string 'No recent system activity recorded.' — it never reads any data and there is no corresponding field in getSystemHealth's response (superadminController.js:432-437 returns only status/uptime/services/resources). It will read 'No recent system activity recorded.' forever, even though SecurityAuditLog rows exist and could populate it.
IMPACT: A card that looks like a live feed is permanently static, adding visual noise and implying a feature that isn't there.
FIX: Either feed the card from the latest few SecurityAuditLog/system events (the data exists via /api/security-logs/) or remove the card. The rest of the page (CPU/mem/heap/network via os.* and process.*, db probe, mail config check) is genuinely real — keep that.

### [79] LOW · unverified · area=system-security · cat=ui-quality
TITLE: System Health uptime counter drifts from the real backend uptime between polls
PAGE: system-health  | COMPONENT: EK_SMS/src/components/superadmin/SASystemHealth.js  | BACKEND: GET /api/system-health/ getSystemHealth uptime (superadminController.js:434)
EVIDENCE: getSystemHealth returns the real `process.uptime()` (superadminController.js:434). The frontend seeds uptimeSecs from it (SASystemHealth.js:34) but then runs a purely cosmetic +1/second client timer (lines 51-54) that keeps incrementing independently; it is only re-synced every 30s by the poll (line 46). Comment at line 50 even calls it 'Live counter for aesthetic uptime'. If the backend restarts (uptime resets to ~0) the displayed value will be wrong until the next poll, and a frozen/background tab will overshoot.
IMPACT: Minor: the headline uptime can briefly misrepresent the true server uptime. Cosmetic, but it is presented as an authoritative metric.
FIX: Drive the displayed uptime from `lastChecked` plus the last server uptime value (compute elapsed since fetch) rather than a free-running client counter, so a backend restart is reflected immediately on the next poll.

### [80] LOW · unverified · area=system-security · cat=data-mismatch
TITLE: Security counter maps 7-day failed-logins into a field named flagged_ips (vestigial mislabel)
PAGE: security-logs  | COMPONENT: EK_SMS/src/components/superadmin/SASecurityLogs.js  | BACKEND: GET /api/security-counters/ -> superadminDataController.getSecurityCounters (line 165)
EVIDENCE: fetchCounters maps `flagged_ips: data.failed_logins_7d` (SASecurityLogs.js:53) — it stuffs the 7-day failed-login count into a state key called flagged_ips. The corresponding stat card was deleted (comment line 92), so the value is currently unused, but the dead mapping remains and is misleadingly named. getSecurityCounters (superadminDataController.js:179-184) emits no flagged_ips at all.
IMPACT: Vestigial/confusing wiring; harmless today because nothing renders it, but a future edit could surface a 'flagged IPs' number that is really a failed-login count.
FIX: Drop the flagged_ips key from the counters state (line 30) and the mapping (line 53) since no card consumes it, or rename to failed_logins_7d if you intend to show it.
